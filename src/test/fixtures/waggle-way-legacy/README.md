# Server proof fixtures

These fixed inputs exercise the existing proof verifier, optional goals, and
community authorization regression tests. They are not a playable campaign and
must not be imported into the game. The only runtime campaign is
`packages/waggle-way/src/content/campaign.ts`, using version-7 adventure maps.
