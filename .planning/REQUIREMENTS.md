# Requirements

## Core Requirements
- **REQ-CORE-001:** The backend API must maintain 100% line, branch, and function test coverage.
- **REQ-CORE-002:** All endpoints must adhere to the `ts-rest` contract schemas defined in `shared/api`.
- **REQ-CORE-003:** Security middleware must be applied strictly to all `/admin` routes.
