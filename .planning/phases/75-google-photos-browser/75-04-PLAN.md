---
phase: 75-google-photos-browser
plan: 04
type: execute
wave: 3
depends_on:
  - 75-01
  - 75-02
  - 75-03
files_modified:
  - src/api/google-photos.ts
autonomous: true
requirements:
  - PHOTO-01
  - PHOTO-02
  - PHOTO-03
  - PHOTO-04
  - PHOTO-05

must_haves:
  truths:
    - "Frontend can fetch Google Photos media items via useGetMediaItems hook"
    - "Frontend can fetch Google Photos albums via useGetAlbums hook"
    - "Query parameters are type-safe (albumId, pageToken, pageSize)"
    - "Response types match schemas from route contracts"
  artifacts:
    - path: "src/api/google-photos.ts"
      provides: "React Query hooks for Photos API endpoints"
      exports:
        - "useGetMediaItems"
        - "useGetAlbums"
        - "GetMediaItemsParams"
        - "GetMediaItemsResponse"
        - "GetAlbumsResponse"
      min_lines: 100
  key_links:
    - from: "src/api/google-photos.ts"
      to: "shared/routes/google-photos.ts"
      via: "Type inference from route contracts"
      pattern: "import.*from.*shared/routes/google-photos"
    - from: "src/api/google-photos.ts"
      to: "functions/api/routes/google-photos/index.ts"
      via: "Hono client API calls"
      pattern: "client.googlePhotos"
---

<objective>
Create type-safe React Query hooks for Google Photos API endpoints, enabling frontend consumption with full TypeScript inference.

Purpose: Provide frontend components with easy access to Google Photos data using React Query, with automatic caching, refetching, and type safety from route contracts.

Output: React Query hooks for media items and albums endpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-google-photos-browser/75-CONTEXT.md
@shared/routes/google-photos.ts
@src/api/google-drive.ts
@src/api/honoClient.ts
@.planning/phases/74-google-drive-doc-browser/74-03-SUMMARY.md

## Pattern from Phase 74-03

From src/api/google-drive.ts:
- Import useQuery from "@tanstack/react-query"
- Import client and unwrapResponse from "./honoClient"
- Create hook with params interface
- Query key: ["google-drive", "files", params]
- Query function: calls client.googleDrive.files.$get({ query: params })
- Export query key for manual invalidation

## Photos API Hooks Pattern

Follow the same pattern for Google Photos:
- useGetMediaItems(params) - fetches from /media endpoint
- useGetAlbums(params) - fetches from /albums endpoint
- Both hooks call client.googlePhotos.media.$get() and client.googlePhotos.albums.$get()
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/api/google-photos.ts with React Query hooks</name>
  <files>src/api/google-photos.ts</files>
  <action>
    Create new file src/api/google-photos.ts following the pattern in src/api/google-drive.ts:

    1. Import useQuery from "@tanstack/react-query"
    2. Import client and unwrapResponse from "./honoClient"

    3. Create GetMediaItemsParams interface:
       - albumId?: string - Filter by album per D-10
       - pageToken?: string - Pagination token
       - pageSize?: number - Items per page (default 25)

    4. Create GetMediaItemsResponse interface:
       - mediaItems: Array<{ id, filename, mimeType, baseUrl, width, height, creationTime, description }>
       - nextPageToken?: string

    5. Create useGetMediaItems hook:
       - Takes optional GetMediaItemsParams
       - Query key: ["google-photos", "media", params]
       - Query function: calls client.googlePhotos.media.$get({ query: params })
       - Unwraps response with type inference from route contract
       - Returns { data, isLoading, error, refetch }

    6. Create GetAlbumsResponse interface:
       - albums: Array<{ id, title, mediaItemsCount, coverPhotoBaseUrl }>
       - nextPageToken?: string

    7. Create useGetAlbums hook:
       - Takes optional params: { pageToken?: string, pageSize?: number }
       - Query key: ["google-photos", "albums", params]
       - Query function: calls client.googlePhotos.albums.$get({ query: params })
       - Unwraps response with type inference from route contract
       - Returns { data, isLoading, error, refetch }

    8. Export query keys for manual invalidation:
       - mediaItemsQueryKey = ["google-photos", "media"]
       - albumsQueryKey = ["google-photos", "albums"]

    9. Follow TypeScript Safety SKILL.md patterns (no `as any`, inferred types)

    Note: Do NOT create upload mutation hook yet — that will be in 75-06
  </action>
  <verify>
    File src/api/google-photos.ts exists with useGetMediaItems and useGetAlbums hooks exported. TypeScript compilation succeeds.
  </verify>
  <done>React Query hooks created for media items and albums endpoints. Hooks provide type-safe access to Google Photos data with automatic caching and refetching.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Hook calls mediated through Hono client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-04-01 | D | Query keys | mitigate | Params included in query key ensures proper cache invalidation |
</threat_model>

<verification>
1. File src/api/google-photos.ts exists
2. useGetMediaItems hook exported with proper type inference
3. useGetAlbums hook exported with proper type inference
4. Query keys include all parameters for proper cache invalidation
5. TypeScript compilation succeeds with inferred types (no `any`)
6. Hooks follow the same pattern as google-drive.ts
</verification>

<success_criteria>
- [x] Frontend can fetch Google Photos media items via useGetMediaItems hook
- [x] Frontend can fetch Google Photos albums via useGetAlbums hook
- [x] Query parameters (albumId, pageToken, pageSize) are type-safe
- [x] Response types match photoMediaItemSchema and photoAlbumSchema from route contracts
- [x] Error handling via unwrapResponse follows ARESWEB patterns
- [x] Query keys exported for manual invalidation
</success_criteria>

<output>
After completion, create `.planning/phases/75-google-photos-browser/75-04-SUMMARY.md`
</output>
