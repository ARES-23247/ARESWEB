---
phase: 75-google-photos-browser
plan: 06
type: execute
wave: 4
depends_on:
  - 75-01
  - 75-05
files_modified:
  - src/api/google-photos.ts
autonomous: true
requirements:
  - UPLOAD-01
  - UPLOAD-03

must_haves:
  truths:
    - "Frontend can upload photos via useUploadPhotos mutation"
    - "Mutation accepts files, title, description, albumId parameters"
    - "Mutation returns uploadedCount and failures array"
    - "Mutation triggers cache invalidation for media items list per D-14"
  artifacts:
    - path: "src/api/google-photos.ts"
      provides: "React Query mutation hook for photo upload"
      exports:
        - "useUploadPhotos"
        - "UploadPhotosParams"
        - "UploadPhotosResponse"
  key_links:
    - from: "src/api/google-photos.ts"
      to: "functions/api/routes/google-photos/index.ts"
      via: "Hono client POST call"
      pattern: "client.googlePhotos.upload.$post"
    - from: "src/api/google-photos.ts"
      to: "@tanstack/react-query QueryClient"
      via: "Cache invalidation after upload"
      pattern: "invalidateQueries.*google-photos"
---

<objective>
Create React Query mutation hook for photo upload functionality, enabling frontend components to upload photos with automatic cache invalidation.

Purpose: Provide a type-safe mutation hook for uploading photos to Google Photos, with automatic refresh of media items list after successful upload per D-14.

Output: React Query mutation hook for photo upload.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-google-photos-browser/75-CONTEXT.md
@shared/routes/google-photos.ts
@src/api/google-photos.ts
@src/api/honoClient.ts
@.planning/phases/74-google-drive-doc-browser/74-03-SUMMARY.md

## Key Decisions from CONTEXT.md

- **D-14:** On success, refresh media list to show new uploads
- **D-21:** Display upload errors inline (file too large, unsupported format, API failure)
- **D-22:** Retry failed uploads with "Retry" button per failed file

## Mutation Pattern from ARESWEB

From src/api/honoClient.ts:
- Use useMutation from "@tanstack/react-query"
- Use unwrapResponse for error handling
- Use withMutationCallbacks for cache invalidation
- Follow TypeScript Safety SKILL.md patterns

## Upload Request Format

For multipart/form-data uploads with Hono client:
- Use FormData to construct request body
- Files need special handling (not JSON serialization)
- Metadata fields (title, description, albumId) as FormData fields
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add upload mutation to src/api/google-photos.ts</name>
  <files>src/api/google-photos.ts</files>
  <action>
    Extend src/api/google-photos.ts with upload mutation hook:

    1. Import useMutation from "@tanstack/react-query"
    2. Import toast and toastApiError from "./honoClient"

    3. Create UploadPhotosParams interface:
       - files: File[] - Array of image files to upload
       - title?: string - Optional title for all photos
       - description?: string - Optional description for all photos
       - albumId?: string - Optional album ID for assignment

    4. Create UploadPhotosResponse interface:
       - uploadedCount: number - Number of successfully uploaded photos
       - failures?: Array<{ filename: string, error: string }> - Per-file failures

    5. Create useUploadPhotos hook:
       - Takes optional UseMutationOptions parameter for callbacks
       - Mutation function:
         - Create FormData from params
         - Append each file to FormData with key "files"
         - Append title, description, albumId if provided
         - Call client.googlePhotos.upload.$post({ body: formData })
         - Use unwrapResponse for type-safe error handling
         - Return response data
       - Use withMutationCallbacks for:
         - onSuccess: Invalidate mediaItemsQueryKey to refresh media list per D-14
         - onError: Display toast error with diagnostic code
       - Returns mutation object (mutate, mutateAsync, isLoading, error)

    6. Follow TypeScript Safety SKILL.md patterns (no `as any`)

    7. Export the hook and types
  </action>
  <verify>
    File src/api/google-photos.ts exists with useUploadPhotos hook exported. TypeScript compilation succeeds.
  </verify>
  <done>React Query mutation hook created for photo upload. Hook accepts files and metadata, uploads to Photos API, and automatically refreshes media items list on success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Mutation calls mediated through Hono client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-06-01 | I | File inputs | mitigate | FormData construction ensures proper content-type header |
| T-75-06-02 | D | Cache invalidation | mitigate | Automatic invalidation ensures UI shows uploaded photos |
</threat_model>

<verification>
1. File src/api/google-photos.ts extended with useUploadPhotos hook
2. Hook accepts File[] array and metadata parameters
3. Hook calls client.googlePhotos.upload.$post() with FormData body
4. onSuccess callback invalidates mediaItemsQueryKey for auto-refresh
5. onError callback displays toast with error details
6. TypeScript compilation succeeds with inferred types
7. Hook follows ARESWEB patterns from honoClient.ts
</verification>

<success_criteria>
- [x] Frontend can upload photos via useUploadPhotos mutation
- [x] Mutation accepts files, title, description, albumId parameters per UPLOAD-03
- [x] Mutation returns uploadedCount and failures array per D-21/D-22
- [x] Mutation triggers cache invalidation for media items list per D-14
- [x] Error handling via toast follows ARESWEB patterns
- [x] Type-safe with no `any` types or @ts-ignore
</success_criteria>

<output>
After completion, create `.planning/phases/75-google-photos-browser/75-06-SUMMARY.md`
</output>
