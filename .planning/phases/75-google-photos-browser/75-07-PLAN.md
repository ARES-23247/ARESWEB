---
phase: 75-google-photos-browser
plan: 07
type: execute
wave: 5
depends_on:
  - 75-04
  - 75-06
files_modified:
  - src/routes/dashboard/photos.tsx
  - src/components/dashboard/PhotoGrid.tsx
  - src/components/dashboard/AlbumSidebar.tsx
  - src/components/dashboard/PhotoUploadModal.tsx
autonomous: false
requirements:
  - PHOTO-01
  - PHOTO-02
  - PHOTO-03
  - PHOTO-04
  - PHOTO-05
  - UPLOAD-01
  - UPLOAD-03

must_haves:
  truths:
    - "User can browse Google Photos media items at /dashboard/photos per PHOTO-01"
    - "Photos display as thumbnails in grid layout with metadata per PHOTO-03"
    - "Only photos shown (no videos) per PHOTO-02"
    - "Albums displayed in sidebar for filtering per PHOTO-05"
    - "Infinite scroll pagination loads more photos per PHOTO-04"
    - "Upload button opens modal with file input, title, description, album select per UPLOAD-03"
    - "Upload shows progress and errors per D-21/D-22"
  artifacts:
    - path: "src/routes/dashboard/photos.tsx"
      provides: "Main dashboard page for Google Photos browser"
      min_lines: 200
    - path: "src/components/dashboard/PhotoGrid.tsx"
      provides: "Photo grid component with infinite scroll"
      contains: "Intersection Observer for pagination"
    - path: "src/components/dashboard/AlbumSidebar.tsx"
      provides: "Album list sidebar with filtering"
    - path: "src/components/dashboard/PhotoUploadModal.tsx"
      provides: "Upload modal with file input and metadata form"
      contains: "Multi-file input, title, description, album select"
  key_links:
    - from: "src/routes/dashboard/photos.tsx"
      to: "src/api/google-photos.ts"
      via: "useGetMediaItems and useGetAlbums hooks"
      pattern: "useGetMediaItems|useGetAlbums"
    - from: "src/components/dashboard/PhotoUploadModal.tsx"
      to: "src/api/google-photos.ts"
      via: "useUploadPhotos mutation"
      pattern: "useUploadPhotos"
    - from: "src/components/dashboard/PhotoGrid.tsx"
      to: "functions/api/routes/google-photos/index.ts"
      via: "Infinite scroll with pageToken pagination"
      pattern: "pageToken|IntersectionObserver"
---

<objective>
Build the Google Photos browser dashboard UI with album sidebar, photo grid with infinite scroll, and upload modal functionality.

Purpose: Provide a complete photo browsing and upload experience that meets all PHOTO and UPLOAD requirements, following ARES brand standards and accessibility guidelines.

Output: Dashboard page at /dashboard/photos with album filtering, photo grid, and upload capability.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-google-photos-browser/75-CONTEXT.md
@src/routes/dashboard/drive-docs.tsx
@src/api/google-photos.ts
@.agents/skills/aresweb-brand-enforcement/SKILL.md
@.agents/skills/aresweb-web-accessibility/SKILL.md

## Pattern from Phase 74-04 (Drive Docs UI)

From src/routes/dashboard/drive-docs.tsx:
- Header with title and refresh button
- Search input with debouncing
- Grid/list view with loading and error states
- Empty state with icon and message
- Pagination footer with "Load more" button
- File type icons and metadata display
- Keyboard navigation support

## Key Decisions from CONTEXT.md

UI Layout (D-16/D-17/D-18):
- Left sidebar: albums list (clickable filters)
- Main area: photo grid with infinite scroll
- Top bar: search input, upload button

Thumbnail Display (D-03/D-04):
- Use Photos API baseUrl with =w200-h200 for thumbnails
- 200x200px in grid view

Albums Display (D-08/D-09/D-10):
- Albums as cards with cover image and title
- Click album to filter media items by albumId

Upload Flow (D-11/D-12/D-13):
- File input accepts multiple image files
- Upload form: title, description, album select
- Upload using mediaItems:batchCreate endpoint

Pagination (D-06/D-07):
- Page size 25 with pageToken
- Grid view loads next page on scroll (infinite scroll)

## Accessibility Requirements

From Web Accessibility SKILL.md:
- WCAG 2.1 AA compliance (4.5:1 contrast)
- Keyboard navigability
- Semantic HTML
- ARIA labels for functional icons
- Focus states for interactive elements

## Brand Requirements

From Brand Enforcement SKILL.md:
- Use ARES color palette (ares-red, ares-gold, obsidian, marble)
- No generic hex codes
- Typography: League Spartan / Inter
- ARES cut corners on containers

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create main photos dashboard page structure</name>
  <files>src/routes/dashboard/photos.tsx</files>
  <action>
    Create new file src/routes/dashboard/photos.tsx following the pattern in src/routes/dashboard/drive-docs.tsx:

    1. Import from @tanstack/react-router: createFileRoute
    2. Import useState, useEffect from react
    3. Import lucide-react icons: Search, Image, Plus, AlertCircle, RefreshCw, X, Upload
    4. Import from src/api/google-photos: useGetMediaItems, useGetAlbums
    5. Export Route with createFileRoute('/dashboard/photos')

    6. Create Photos component with:
       - State for searchQuery, selectedAlbumId
       - useGetMediaItems hook with albumId and pageToken params
       - useGetAlbums hook for sidebar
       - Loading, error, and empty state handling

    7. Build header with:
       - Title: "Google Photos"
       - Description: "Browse and upload team photos."
       - Search input with icon
       - Upload button (opens modal per Task 4)
       - Refresh button for manual refetch

    8. Follow ARES Brand Enforcement SKILL.md (ares-red, obsidian, marble colors)
    9. Follow Web Accessibility SKILL.md (WCAG 2.1 AA, keyboard nav, ARIA labels)

    Note: Sidebar and grid will be separate components (Tasks 2-3)
  </action>
  <verify>
    File exists at src/routes/dashboard/photos.tsx. Page renders at /dashboard/photos route.
  </verify>
  <done>Main photos dashboard page structure created with header, search, and state management. Ready for sidebar and grid components.</done>
</task>

<task type="auto">
  <name>Task 2: Create AlbumSidebar component</name>
  <files>src/components/dashboard/AlbumSidebar.tsx</files>
  <action>
    Create new file src/components/dashboard/AlbumSidebar.tsx:

    1. Import from react: useState
    2. Import lucide-react icons: Image as default icon

    3. Define props interface:
       - albums: Array<{ id, title, coverPhotoBaseUrl, mediaItemsCount }>
       - selectedAlbumId: string | null
       - onAlbumSelect: (albumId: string | null) => void
       - isLoading: boolean

    4. Create AlbumSidebar component:
       - Render "All Photos" option at top (albumId = null)
       - Map albums to sidebar items
       - Each album item:
         - Cover photo (coverPhotoBaseUrl with =w100-h100)
         - Album title
         - Item count badge
         - Selected state styling (ares-red background)
         - Click handler calls onAlbumSelect
       - Loading skeleton while fetching
       - Empty state when no albums

    5. Styling per ARES brand:
       - Dark background (obsidian)
       - ARES red accent for selected album
       - Hover effects on album items
       - Proper contrast ratios (4.5:1 minimum)

    6. Accessibility:
       - Semantic nav element
       - ARIA current for selected album
       - Keyboard navigation (arrow keys)
       - Focus indicators
  </action>
  <verify>
    File exists at src/components/dashboard/AlbumSidebar.tsx. Component renders albums with cover photos and selection state.
  </verify>
  <done>AlbumSidebar component created with clickable album cards, cover photos, item counts, and keyboard navigation.</done>
</task>

<task type="auto">
  <name>Task 3: Create PhotoGrid component with infinite scroll</name>
  <files>src/components/dashboard/PhotoGrid.tsx</files>
  <action>
    Create new file src/components/dashboard/PhotoGrid.tsx:

    1. Import from react: useState, useRef, useEffect
    2. Import lucide-react icons: ZoomIn

    3. Define props interface:
       - mediaItems: Array<{ id, filename, baseUrl, width, height, creationTime }>
       - isLoading: boolean
       - hasNextPage: boolean
       - onLoadMore: () => void
       - onPhotoClick: (mediaItem) => void

    4. Create PhotoGrid component:
       - CSS grid layout (responsive columns)
       - Each photo item:
         - Thumbnail image: baseUrl with =w200-h200 for 200x200px thumbnails per D-04
         - Hover overlay with ZoomIn icon
         - Click handler for photo detail view
         - Lazy loading for images
       - Intersection Observer for infinite scroll per D-07:
         - Trigger onLoadMore when last photo enters viewport
         - Loading spinner at bottom when fetching next page
       - Empty state with icon and message
       - Loading skeleton state

    5. Styling per ARES brand:
       - Dark background (obsidian)
       - ARES red accent on hover
       - Consistent grid spacing
       - Proper contrast ratios

    6. Accessibility:
       - Semantic grid with proper heading hierarchy
       - Alt text for images (filename)
       - ARIA labels for interactive elements
       - Keyboard navigation (arrow keys through grid)
       - Focus indicators on photos
  </action>
  <verify>
    File exists at src/components/dashboard/PhotoGrid.tsx. Component renders photo grid with infinite scroll pagination.
  </verify>
  <done>PhotoGrid component created with thumbnail display, infinite scroll via Intersection Observer, and keyboard navigation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dashboard page with album sidebar and photo grid components. All functionality implemented per requirements.</what-built>
  <how-to-verify>
    1. Navigate to https://aresweb.pages.dev/dashboard/photos
    2. Verify page loads with header containing search and upload button
    3. Verify left sidebar displays albums with cover photos
    4. Verify clicking an album filters the photo grid
    5. Verify clicking "All Photos" shows all photos
    6. Verify photo grid displays thumbnails (200x200px)
    7. Verify scrolling to bottom loads more photos (infinite scroll)
    8. Verify search input filters photos by filename
    9. Verify upload button opens modal (from Task 4)
    10. Test keyboard navigation: Tab through albums and photos, Enter to select
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Create PhotoUploadModal component</name>
  <files>src/components/dashboard/PhotoUploadModal.tsx</files>
  <action>
    Create new file src/components/dashboard/PhotoUploadModal.tsx:

    1. Import from react: useState, useRef
    2. Import lucide-react icons: X, Upload, AlertCircle
    3. Import from src/api/google-photos: useUploadPhotos
    4. Import albums prop for album selection

    5. Define props interface:
       - isOpen: boolean
       - onClose: () => void
       - albums: Array<{ id, title }>

    6. Create PhotoUploadModal component:
       - Modal dialog with backdrop
       - Close button (X icon)
       - File input (multiple, accept="image/*" per D-11)
       - Selected files preview list with:
         - File name
         - File size
         - Remove button per file
       - Metadata form:
         - Title input (optional)
         - Description textarea (optional)
         - Album select dropdown (optional per D-12)
       - Upload button with loading state
       - Progress indicator during upload
       - Error display per file (D-21)
       - Retry button for failed uploads (D-22)
       - Success message with uploadedCount
       - Auto-close on success after 2 seconds

    7. useUploadPhotos integration:
       - Pass files, title, description, albumId to mutation
       - Display uploadedCount on success
       - Display failures array with errors
       - Refresh media list on success (automatic via hook)

    8. Styling per ARES brand:
       - Modal with obsidian background
       - ARES red upload button
       - Proper spacing and form layout
       - Error states with red accent

    9. Accessibility:
       - Dialog with proper role and ARIA attributes
       - Focus trap within modal
       - Escape key closes modal
       - Form labels associated with inputs
       - Error announcements to screen readers

    10. Integrate modal into main photos page:
       - Add state for isUploadModalOpen
       - Upload button in header opens modal
       - Pass albums list to modal
  </action>
  <verify>
    File exists at src/components/dashboard/PhotoUploadModal.tsx. Modal opens from upload button, accepts files and metadata, and uploads successfully.
  </verify>
  <done>PhotoUploadModal component created with multi-file input, metadata form, album selection, progress display, and error handling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Upload modal with file selection, metadata entry, and album assignment. Full upload flow implemented.</what-built>
  <how-to-verify>
    1. Navigate to https://aresweb.pages.dev/dashboard/photos
    2. Click "Upload" button in header
    3. Verify modal opens with backdrop
    4. Verify file input accepts multiple image files (JPG, PNG, WEBP)
    5. Verify selected files display in preview list
    6. Verify title and description fields work
    7. Verify album dropdown shows available albums
    8. Test upload with a small image file (< 1MB)
    9. Verify progress indicator shows during upload
    10. Verify success message displays with uploaded count
    11. Verify uploaded photo appears in grid (auto-refresh per D-14)
    12. Test error handling: upload a non-image file (should reject)
    13. Test keyboard: Tab through form, Enter to submit, Escape to close modal
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→UI | Form input validation before API calls |
| UI→API | All calls protected by admin middleware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-07-01 | I | File input | mitigate | accept="image/*" attribute restricts file picker, server-side validation per 75-05 |
| T-75-07-02 | X | Upload form | mitigate | All API calls use Hono client with auth headers, no client-side secrets |
| T-75-07-03 | T | Modal state | mitigate | Admin middleware enforced at API level, UI is client-side only |
</threat_model>

<verification>
1. Page renders at /dashboard/photos route
2. Album sidebar displays albums with cover photos and selection
3. Photo grid displays thumbnails (200x200px) with infinite scroll
4. Search input filters photos by filename
5. Upload button opens modal with file input and metadata form
6. Upload accepts multiple image files with validation
7. Upload displays progress and errors per file
8. Successful upload refreshes photo grid automatically
9. All components follow ARES brand standards
10. All components meet WCAG 2.1 AA accessibility requirements
11. Keyboard navigation works throughout (albums, grid, modal)
12. Focus indicators visible on all interactive elements
</verification>

<success_criteria>
- [x] User can browse Google Photos media items at /dashboard/photos per PHOTO-01
- [x] Only photos shown (no videos) per PHOTO-02
- [x] Photos display as thumbnails with metadata per PHOTO-03
- [x] Infinite scroll pagination works per PHOTO-04
- [x] Albums displayed in sidebar with filtering per PHOTO-05
- [x] Upload button opens modal per UPLOAD-01
- [x] Upload accepts files, title, description, album selection per UPLOAD-03
- [x] Upload shows progress and errors (D-21/D-22)
- [x] Upload refreshes grid on success (D-14)
- [x] All components follow ARES brand guidelines
- [x] All components meet WCAG 2.1 AA accessibility standards
</success_criteria>

<output>
After completion, create `.planning/phases/75-google-photos-browser/75-07-SUMMARY.md`
</output>
