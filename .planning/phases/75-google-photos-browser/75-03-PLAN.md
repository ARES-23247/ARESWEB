---
phase: 75-google-photos-browser
plan: 03
type: tdd
wave: 2
depends_on:
  - 75-01
files_modified:
  - functions/api/routes/google-photos/index.ts
  - functions/api/routes/google-photos/index.test.ts
autonomous: true
requirements:
  - PHOTO-05

must_haves:
  truths:
    - "GET /api/google-photos/albums returns Google Photos albums"
    - "Response includes album id, title, cover photo URL, and item count per PHOTO-05"
    - "Pagination supported via pageToken parameter"
  artifacts:
    - path: "functions/api/routes/google-photos/index.ts"
      provides: "GET /albums endpoint implementation"
      contains: "GET /albums handler (replaces placeholder from Phase 73)"
    - path: "functions/api/routes/google-photos/index.test.ts"
      provides: "Test coverage for /albums endpoint"
  key_links:
    - from: "functions/api/routes/google-photos/index.ts"
      to: "https://photoslibrary.googleapis.com/v1/albums"
      via: "Photos API fetch call"
      pattern: "fetch.*photoslibrary.*albums"
---

<objective>
Implement GET /albums endpoint for Google Photos API using TDD methodology. The endpoint lists albums with cover photos and item counts for organized browsing.

Purpose: Provide access to Google Photos albums so users can filter media items by album, enabling organized photo browsing per PHOTO-05.

Output: Working GET /albums endpoint with full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-google-photos-browser/75-CONTEXT.md
@shared/routes/google-photos.ts
@functions/api/routes/google-photos/index.ts
@functions/utils/googleAuth.ts
@.planning/phases/74-google-drive-doc-browser/74-02-SUMMARY.md

## Existing Code to Reference

From functions/api/routes/google-photos/index.ts (Phase 73):
- /albums placeholder exists at line 119-128
- Returns empty array [] with TODO comment for Phase 75 implementation

## Photos API Documentation

- albums:list endpoint: GET https://photoslibrary.googleapis.com/v1/albums
- Query parameters: pageSize (number), pageToken (string)
- Response: { albums: [{ id, title, mediaItemsCount, coverPhotoBaseUrl }, ...], nextPageToken?: string }

## Key Decisions from CONTEXT.md

- **D-08:** Fetch albums using Photos Library API albums.list() endpoint
- **D-09:** Display albums as cards with cover image and title
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for GET /albums endpoint (RED phase)</name>
  <files>functions/api/routes/google-photos/index.test.ts</files>
  <behavior>
    - Test 1: GET /albums returns 200 with albums array (even when empty)
    - Test 2: Response includes album fields: id, title, mediaItemsCount, coverPhotoBaseUrl
    - Test 3: Pagination works with pageToken parameter
    - Test 4: PageSize parameter can be customized
    - Test 5: Admin middleware blocks unauthenticated requests (401)
    - Test 6: Photos API errors are handled with proper ApiError
  </behavior>
  <action>
    Add failing tests to functions/api/routes/google-photos/index.test.ts:

    1. Mock fetch to simulate Photos API albums responses:
       - Success case with albums array
       - Success case with nextPageToken for pagination
       - Empty albums array
    2. Write test for successful albums listing returning album fields
    3. Write test for pagination with pageToken
    4. Write test for pageSize customization
    5. Write test for 401 unauthorized without admin session
    6. Write test for Photos API error handling (502 response)

    All tests must FAIL initially (RED phase).
  </action>
  <verify>
    <automated>npm test -- functions/api/routes/google-photos/index.test.ts</automated>
  </verify>
  <done>All 6 tests fail as expected. Tests cover: albums listing, pagination, pageSize, auth, and error handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GET /albums endpoint (GREEN phase)</name>
  <files>functions/api/routes/google-photos/index.ts</files>
  <behavior>
    - Endpoint accepts query parameters: pageToken, pageSize
    - Calls Photos API albums:list endpoint
    - Returns albums array with cover photo URLs and item counts
  </behavior>
  <action>
    Replace placeholder /albums route in functions/api/routes/google-photos/index.ts (lines 119-128):

    1. Import listAlbumsRoute from shared/routes/google-photos.ts
    2. Implement GET /albums handler:
       - Extract query parameters: pageToken, pageSize
       - Call getPhotosAccessToken(db, env) for authenticated API access
       - Build search params: new URLSearchParams({ pageSize: pageSize || "25" })
       - Add pageToken if provided
       - GET from https://photoslibrary.googleapis.com/v1/albums with search params
       - Handle authentication failures (401) and API errors (502)
       - Transform Photos API response to match listAlbumsResponseSchema:
         - Map albums array to include id, title, mediaItemsCount, coverPhotoBaseUrl
         - Include nextPageToken if present
       - Return transformed response

    3. Ensure all tests pass (GREEN phase)
  </action>
  <verify>
    <automated>npm test -- functions/api/routes/google-photos/index.test.ts</automated>
  </verify>
  <done>All tests pass. GET /albums endpoint returns albums with cover photos and item counts per PHOTO-05. Pagination supported via pageToken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted query parameters for pageToken, pageSize |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-03-01 | I | pageSize | mitigate | Zod schema coercion min(1).max(50) prevents abuse |
| T-75-03-02 | D | Response data | mitigate | Server-side validation ensures only Photos API data returned |
</threat_model>

<verification>
1. All tests pass for GET /albums endpoint
2. Endpoint returns albums array with: id, title, mediaItemsCount, coverPhotoBaseUrl
3. Pagination functional via pageToken and pageSize
4. 401 response for unauthenticated requests
5. Proper error handling for Photos API failures
6. Placeholder implementation from Phase 73 replaced with full implementation
</verification>

<success_criteria>
- [x] GET /api/google-photos/albums returns Google Photos albums per PHOTO-05
- [x] Response includes album metadata: id, title, coverPhotoBaseUrl, mediaItemsCount
- [x] Pagination supported with pageToken
- [x] All tests pass with full coverage
- [x] RED→GREEN commit sequence maintained
</success_criteria>

<output>
After completion, create `.planning/phases/75-google-photos-browser/75-03-SUMMARY.md`

TDD Gates:
- RED commit: test(75-03): add failing tests for GET /albums endpoint
- GREEN commit: feat(75-03): implement GET /albums endpoint
</output>
