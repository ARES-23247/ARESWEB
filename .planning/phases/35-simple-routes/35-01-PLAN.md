---
phase: 35
wave: 1
depends_on: [34]
files_modified:
  - shared/schemas/contracts/awardsContract.ts
  - shared/schemas/contracts/badgesContract.ts
  - shared/schemas/contracts/commentsContract.ts
  - shared/schemas/contracts/entitiesContract.ts
  - functions/api/routes/awards.ts
  - functions/api/routes/badges.ts
  - functions/api/routes/comments.ts
  - functions/api/routes/entities.ts
  - src/components/AwardEditor.tsx
  - src/components/BadgeManager.tsx
  - src/components/LinkedEntities.tsx
autonomous: true
---

# Phase 35-01: Simple Routes Wave 1

## Goal
Migrate the first 4 simple routes (`awards`, `badges`, `comments`, `entities`) to `@hono/zod-openapi` and decouple their frontend components from `ts-rest`.

## Tasks

<task>
  <action>
    Convert `awardsContract.ts`, `badgesContract.ts`, `commentsContract.ts`, and `entitiesContract.ts` to export `@hono/zod-openapi` `createRoute` instances instead of `initContract().router()`.
  </action>
</task>

<task>
  <action>
    Update the corresponding backend handlers in `functions/api/routes/` to use `OpenAPIHono` and remove all `ts-rest` imports. Fix parameter validation using `c.req.valid('param')` and `c.req.valid('json')`.
  </action>
</task>

<task>
  <action>
    Remove the migrated contracts from `shared/schemas/contracts/index.ts`.
  </action>
</task>

<task>
  <action>
    Decouple the frontend. Update `AwardEditor.tsx`, `BadgeManager.tsx`, and `LinkedEntities.tsx` to replace `api.<route>.useQuery` and `api.<route>.useMutation` with `@tanstack/react-query` hooks using `fetchJson`.
  </action>
</task>

<task>
  <action>
    Run `npx tsc --noEmit` and `npx vitest run` to ensure zero regressions.
  </action>
</task>
