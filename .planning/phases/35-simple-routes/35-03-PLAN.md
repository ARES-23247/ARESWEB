---
phase: 35
wave: 3
depends_on: [35-02]
files_modified:
  - shared/schemas/contracts/seasonsContract.ts
  - shared/schemas/contracts/settingsContract.ts
  - shared/schemas/contracts/socialQueueContract.ts
  - shared/schemas/contracts/storeContract.ts
  - functions/api/routes/seasons.ts
  - functions/api/routes/settings.ts
  - functions/api/routes/socialQueue.ts
  - functions/api/routes/store.ts
autonomous: true
---

# Phase 35-03: Simple Routes Wave 3

## Goal
Migrate `seasons`, `settings`, `socialQueue`, and `store` routes to native OpenAPI and decouple their frontend components.

## Tasks

<task>
  <action>
    Convert the 4 contract files to `createRoute` instances.
  </action>
</task>

<task>
  <action>
    Update the backend handlers in `functions/api/routes/` to use `OpenAPIHono`.
  </action>
</task>

<task>
  <action>
    Remove the migrated contracts from `shared/schemas/contracts/index.ts`.
  </action>
</task>

<task>
  <action>
    Decouple the frontend components: `SeasonManagerTab.tsx`, `SeasonPicker.tsx`, `SeasonEditor.tsx`, `Seasons.tsx`, `useAdminSettings.ts`, `SocialCalendar.tsx`, `SocialAnalytics.tsx`, `SocialComposer.tsx`, `Store.tsx`, `StoreOrders.tsx`, and `CartDrawer.tsx`.
  </action>
</task>

<task>
  <action>
    Run `npx tsc --noEmit` and `npx vitest run`.
  </action>
</task>
