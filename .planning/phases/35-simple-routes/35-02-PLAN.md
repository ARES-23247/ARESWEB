---
phase: 35
wave: 2
depends_on: [35-01]
files_modified:
  - shared/schemas/contracts/locationsContract.ts
  - shared/schemas/contracts/logisticsContract.ts
  - shared/schemas/contracts/notificationsContract.ts
  - shared/schemas/contracts/pointsContract.ts
  - functions/api/routes/locations.ts
  - functions/api/routes/logistics.ts
  - functions/api/routes/notifications.ts
  - functions/api/routes/points.ts
autonomous: true
---

# Phase 35-02: Simple Routes Wave 2

## Goal
Migrate `locations`, `logistics`, `notifications`, and `points` routes to native OpenAPI and decouple their frontend components.

## Tasks

<task>
  <action>
    Convert the 4 contract files to `createRoute` instances.
  </action>
</task>

<task>
  <action>
    Update the backend handlers in `functions/api/routes/` to use `OpenAPIHono`.
  </action>
</task>

<task>
  <action>
    Remove the migrated contracts from `shared/schemas/contracts/index.ts`.
  </action>
</task>

<task>
  <action>
    Decouple the frontend components: `CreateLocationModal.tsx`, `LocationsManager.tsx`, `DietarySummary.tsx`, `useDashboardNotifications.ts`, `useMergedNotifications.ts`, `Navbar.tsx`, `ProfilePage.tsx`, `Leaderboard.tsx`, and `AdminUsers.tsx`. Replace ts-rest hooks with `@tanstack/react-query` and `fetchJson`.
  </action>
</task>

<task>
  <action>
    Run `npx tsc --noEmit` and `npx vitest run`.
  </action>
</task>
