---
phase: 73-service-account-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/utils/gcalSync.ts
  - wrangler.toml
autonomous: true
requirements:
  - AUTH-01
  - UPLOAD-02
user_setup:
  - service: google_cloud
    why: "Service account requires Photos and Drive API scopes added"
    dashboard_config:
      - task: "Add Photos Library API scope to service account"
        location: "Google Cloud Console -> APIs & Services -> Credentials -> Edit Service Account -> Scopes"
      - task: "Enable Google Photos Library API"
        location: "Google Cloud Console -> APIs & Services -> Library"
      - task: "Enable Google Drive API"
        location: "Google Cloud Console -> APIs & Services -> Library"
    env_vars:
      - name: GCAL_SERVICE_ACCOUNT_EMAIL
        source: "Existing Google Cloud Service Account (reused per D-01)"
      - name: GCAL_PRIVATE_KEY
        source: "Existing Google Cloud Service Account (reused per D-01)"

must_haves:
  truths:
    - "Service account JWT includes Calendar, Photos, and Drive scopes"
    - "getGcalAccessToken() generates valid tokens for all three APIs"
    - "Photos write scope (photoslibrary.edit) is included for upload capability per D-11"
  artifacts:
    - path: "functions/utils/gcalSync.ts"
      provides: "JWT token generation with unified scopes per D-02"
      exports: ["getGcalAccessToken", "GCalConfig"]
      min_lines: 60
    - path: "wrangler.toml"
      provides: "Environment variable bindings"
      contains: "GCAL_SERVICE_ACCOUNT_EMAIL, GCAL_PRIVATE_KEY"
  key_links:
    - from: "getGcalAccessToken()"
      to: "https://oauth2.googleapis.com/token"
      via: "POST with JWT assertion"
      pattern: "fetch.*oauth2\\.googleapis\\.com/token"

---

<objective>
Extend service account authentication to support Google Photos Library API and Google Drive API alongside existing Google Calendar integration.

Purpose: Enable API access to Photos (read + write for upload) and Drive (read-only) using the existing service account credential infrastructure, avoiding duplicate credential management.

Output: Updated `getGcalAccessToken()` function that generates JWTs with unified scopes for Calendar, Photos, and Drive APIs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-service-account-auth/73-CONTEXT.md
@.planning/REQUIREMENTS.md
@functions/utils/gcalSync.ts
@shared/errors/api.ts
@.agents/skills/aresweb-typescript-safety/SKILL.md
@.agents/skills/aresweb-error-architecture/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend JWT scope to include Photos and Drive APIs</name>
  <files>functions/utils/gcalSync.ts</files>
  <read_first>
    functions/utils/gcalSync.ts
  </read_first>
  <behavior>
    - Test 1: getGcalAccessToken() returns JWT with scope containing calendar scope
    - Test 2: getGcalAccessToken() returns JWT with scope containing photoslibrary.edit scope (write access per D-11)
    - Test 3: getGcalAccessToken() returns JWT with scope containing drive.readonly scope
    - Test 4: Token successfully exchanges at oauth2.googleapis.com/token endpoint
  </behavior>
  <action>
    Modify the `getGcalAccessToken()` function in `functions/utils/gcalSync.ts` to include unified scopes per D-02:

    1. Update the JWT scope claim from single calendar scope to unified scope string:
       - Keep existing: `https://www.googleapis.com/auth/calendar`
       - Add Photos read scope: `https://www.googleapis.com/auth/photoslibrary.readonly`
       - Add Photos write scope: `https://www.googleapis.com/auth/photoslibrary.appendonly` (per D-11, enables upload)
       - Add Drive read scope: `https://www.googleapis.com/auth/drive.readonly`

    2. Scope string format: Space-delimited string in the JWT payload
       Example: `https://www.googleapis.com/auth/calendar https://www.googleapis.com/auth/drive.readonly https://www.googleapis.com/auth/photoslibrary.readonly https://www.googleapis.com/auth/photoslibrary.appendonly`

    3. Do NOT modify the `GCalConfig` interface type (already has email, privateKey, calendarId fields)
    4. Do NOT modify the JWT signing logic (RS256 with importPKCS8)
    5. Do NOT modify the token exchange endpoint (https://oauth2.googleapis.com/token)
    6. Preserve literal \n handling in private key (line 31: `replace(/\\n/g, "\n")`)

    The function signature `getGcalAccessToken(config: GCalConfig): Promise<string>` remains unchanged.
  </action>
  <verify>
    <automated>npm test -- --grep="getGcalAccessToken"</automated>
  </verify>
  <done>
    - getGcalAccessToken() generates JWT with unified scopes
    - Token exchange returns valid access_token for Calendar, Drive, and Photos APIs
    - Existing calendar sync continues to work (no regression)
    - Photos scope includes write capability (photoslibrary.appendonly per D-11)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloudflare Workers → Google OAuth | Service account JWT assertion exchanged for access token |
| D1 settings → token storage | OAuth tokens stored in D1 (plaintext, acceptable for service account) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | S | Service account private key | mitigate | Private key stored as Cloudflare Worker secret (encrypted at rest), never exposed to client |
| T-73-02 | T | OAuth token in transit | mitigate | HTTPS enforced for all Google API calls (oauth2.googleapis.com) |
| T-73-03 | I | JWT scope manipulation | accept | Service account auth (not user OAuth), scopes hardcoded in server code |
| T-73-04 | D | Private key exposure in logs | mitigate | Ensure GCAL_PRIVATE_KEY never logged; jose library handles keys securely |
| T-73-05 | E | Token exhaustion | accept | Service account has 100K daily quota, sufficient for team usage |
| T-73-06 | R | Scope creep (excessive permissions) | mitigate | Using read-only scopes for Drive, append-only for Photos (minimal required per D-11) |
</threat_model>

<verification>
- JWT includes all four required scopes (calendar, drive.readonly, photoslibrary.readonly, photoslibrary.appendonly)
- Token successfully exchanges at Google OAuth endpoint
- Existing calendar integration tests pass (no regression)
- Photos write scope enables upload capability per UPLOAD-02 requirement
</verification>

<success_criteria>
1. getGcalAccessToken() returns JWT with unified scope string
2. Token exchange returns HTTP 200 with access_token
3. Access token valid for Calendar, Drive, and Photos API calls
4. Zero changes to existing calendar sync functionality
</success_criteria>

<output>
After completion, create `.planning/phases/73-service-account-auth/73-01-SUMMARY.md`
</output>
