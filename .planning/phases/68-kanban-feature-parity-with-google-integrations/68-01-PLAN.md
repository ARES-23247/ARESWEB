---
phase: 68-kanban-feature-parity-with-google-integrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.ts, shared/routes/tasks.ts, functions/api/routes/tasks.ts]
autonomous: true
requirements: [REQ-68-01]
user_setup: []

must_haves:
  truths:
    - "Database schema includes labels, checklists, attachments, and new task fields"
    - "API routes correctly process new task fields (startDate, estimatedMinutes, coverImage)"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Drizzle table definitions for tasks extension"
    - path: "shared/routes/tasks.ts"
      provides: "Zod schemas for expanded task contracts"
    - path: "functions/api/routes/tasks.ts"
      provides: "API endpoints to handle new fields and relations"
  key_links:
    - from: "functions/api/routes/tasks.ts"
      to: "src/db/schema.ts"
      via: "drizzle inserts and updates"
---

<objective>
Update the Drizzle schema and API endpoints to support Kanban feature parity metadata.

Purpose: Foundation for Checklist, Labels, and Dates/Estimates functionality.
Output: Updated database schema and expanded API route handlers.
</objective>

<execution_context>
@$HOME/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-kanban-feature-parity-with-google-integrations/68-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Drizzle Schema</name>
  <files>src/db/schema.ts</files>
  <read_first>src/db/schema.ts</read_first>
  <action>
    Modify `src/db/schema.ts`:
    1. Add `startDate: text("start_date")`, `estimatedMinutes: integer("estimated_minutes")`, and `coverImage: text("cover_image")` to the `tasks` table.
    2. Add new `labels` table: `id` (text, PK), `name` (text, notNull), `colorTheme` (text).
    3. Add new `taskLabels` table: `taskId` (text), `labelId` (text), with composite PK or individual indexes.
    4. Add new `taskChecklists` table: `id` (text, PK), `taskId` (text), `content` (text, notNull), `isCompleted` (integer, default 0), `sortOrder` (integer, default 0).
    5. Add new `taskAttachments` table: `id` (text, PK), `taskId` (text), `url` (text, notNull), `title` (text, notNull), `type` (text, notNull), `thumbnailUrl` (text), `createdAt` (text).
  </action>
  <verify>npm run db:generate succeeds</verify>
  <acceptance_criteria>
    - `tasks` table includes `startDate`, `estimatedMinutes`, `coverImage`
    - `labels`, `taskLabels`, `taskChecklists`, `taskAttachments` are exported
  </acceptance_criteria>
  <done>Schema compiles and migrations can be generated</done>
</task>

<task type="auto">
  <name>Task 2: Update Shared API Contracts</name>
  <files>shared/routes/tasks.ts</files>
  <read_first>shared/routes/tasks.ts, src/db/schema-zod.ts</read_first>
  <action>
    Modify `shared/routes/tasks.ts`:
    1. Update `createTaskSchema` and `updateTaskSchema` to accept `startDate`, `estimatedMinutes`, and `coverImage` (all optional).
    2. Extend `TaskSchema` to include these new fields.
    3. Optionally define Zod schemas for Checklist, Label, and Attachment responses if returning them nested. Let's make `checklists`, `labels`, `attachments` arrays nullable/optional on `TaskSchema`.
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <acceptance_criteria>
    - `createTaskSchema` has `startDate`, `estimatedMinutes`, `coverImage`
    - `TaskSchema` correctly types the new fields
  </acceptance_criteria>
  <done>Zod contracts updated without breaking existing types</done>
</task>

<task type="auto">
  <name>Task 3: Update Backend Tasks Router</name>
  <files>functions/api/routes/tasks.ts</files>
  <read_first>functions/api/routes/tasks.ts</read_first>
  <action>
    Modify `functions/api/routes/tasks.ts`:
    1. In `createTaskRoute` handler, extract `startDate`, `estimatedMinutes`, `coverImage` from `body` and insert into `schema.tasks`.
    2. In `updateTaskRoute` handler, support updating `startDate`, `estimatedMinutes`, `coverImage`.
    3. In `listTasksRoute` handler, select these new columns so they are returned to the frontend.
  </action>
  <verify>npm run test</verify>
  <acceptance_criteria>
    - `createTaskRoute` saves the new metadata
    - `updateTaskRoute` allows modification of the new metadata
  </acceptance_criteria>
  <done>Backend correctly persists new fields</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run db:generate` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run test` passes
</verification>

<success_criteria>
- All tasks completed
- Database schema matches requirements
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/68-kanban-feature-parity-with-google-integrations/68-01-SUMMARY.md`
</output>
