---
phase: 68-kanban-feature-parity-with-google-integrations
plan: 02
type: execute
wave: 2
depends_on: ["68-01"]
files_modified: [shared/routes/tasks.ts, functions/api/routes/tasks.ts]
autonomous: true
requirements: [REQ-68-02, REQ-68-04]
user_setup: []

must_haves:
  truths:
    - "Users can add checklists to tasks"
    - "Users can attach Google links and see them unfurl server-side"
    - "Zulip notifications fire when attachments are added or checklists complete"
  artifacts:
    - path: "functions/api/routes/tasks.ts"
      provides: "Sub-resource endpoints for checklists, labels, and attachments"
  key_links:
    - from: "functions/api/routes/tasks.ts"
      to: "sendZulipMessage"
      via: "webhook dispatch on milestone"
---

<objective>
Implement API sub-routes for Kanban sub-resources and Google Link server-side unfurling.

Purpose: Enable rich task interactions (attachments, checklists) without client-side CORS issues.
Output: New REST endpoints for Task Attachments, Checklists, Labels, and Dependencies.
</objective>

<execution_context>
@$HOME/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-kanban-feature-parity-with-google-integrations/68-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sub-Resource Contracts</name>
  <files>shared/routes/tasks.ts</files>
  <read_first>shared/routes/tasks.ts</read_first>
  <action>
    Modify `shared/routes/tasks.ts`:
    1. Define `createTaskAttachmentRoute` (`POST /api/tasks/:id/attachments` with url body).
    2. Define `deleteTaskAttachmentRoute` (`DELETE /api/tasks/:id/attachments/:attachmentId`).
    3. Define `createTaskChecklistRoute` (`POST /api/tasks/:id/checklists` with content body).
    4. Define `updateTaskChecklistRoute` (`PATCH /api/tasks/:id/checklists/:checklistId`).
    5. Define `deleteTaskChecklistRoute` (`DELETE /api/tasks/:id/checklists/:checklistId`).
    6. Define `setTaskLabelsRoute` (`POST /api/tasks/:id/labels` to overwrite label assignments).
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <acceptance_criteria>
    - All sub-resource Zod contracts are defined and exported
  </acceptance_criteria>
  <done>Contracts defined for attachments, checklists, and labels</done>
</task>

<task type="auto">
  <name>Task 2: Implement Checklist & Label Endpoints</name>
  <files>functions/api/routes/tasks.ts</files>
  <read_first>functions/api/routes/tasks.ts</read_first>
  <action>
    Modify `functions/api/routes/tasks.ts`:
    1. Implement the Checklist endpoints (CRUD).
    2. On `PATCH /api/tasks/:id/checklists/:checklistId`, if `isCompleted` transitions to 1, check if ALL checklists for this task are now completed. If so, call `sendZulipMessage` to notify the team of the milestone.
    3. Implement `setTaskLabelsRoute` to delete and re-insert into `taskLabels`.
  </action>
  <verify>npm run test</verify>
  <acceptance_criteria>
    - Endpoints persist to `taskChecklists` and `taskLabels`
    - Milestone logic is present for Zulip completion notification
  </acceptance_criteria>
  <done>Checklists and Labels are manageable</done>
</task>

<task type="auto">
  <name>Task 3: Implement Server-Side Unfurling</name>
  <files>functions/api/routes/tasks.ts</files>
  <read_first>functions/api/routes/tasks.ts</read_first>
  <action>
    Modify `functions/api/routes/tasks.ts`:
    1. Implement `createTaskAttachmentRoute`.
    2. In the handler, use `fetch(body.url)` to get the HTML.
    3. Use a regex (e.g., `/<title[^>]*>([^<]+)<\/title>/i`) to extract the title. Fallback to the domain name.
    4. Detect Google types (e.g., if `docs.google.com/document`, type='document', etc.).
    5. Save to `taskAttachments`.
    6. Dispatch `sendZulipMessage` to announce the attachment.
  </action>
  <verify>npm run test</verify>
  <acceptance_criteria>
    - Unfurling uses server-side `fetch`
    - Zulip notification fires on successful attachment
  </acceptance_criteria>
  <done>Attachments are processed and unfurled</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run test` passes
</verification>

<success_criteria>
- All tasks completed
- Sub-resources are addressable via REST
- Server-side unfurling functions
</success_criteria>

<output>
After completion, create `.planning/phases/68-kanban-feature-parity-with-google-integrations/68-02-SUMMARY.md`
</output>
