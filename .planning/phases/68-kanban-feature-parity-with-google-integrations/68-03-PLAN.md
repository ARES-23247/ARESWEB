---
phase: 68-kanban-feature-parity-with-google-integrations
plan: 03
type: execute
wave: 3
depends_on: ["68-02"]
files_modified: [src/components/kanban/TaskDetailsModal.tsx, src/components/kanban/GenericKanbanBoard.tsx, src/api/tasks.ts]
autonomous: true
requirements: [REQ-68-03, REQ-68-05]
user_setup: []

must_haves:
  truths:
    - "Users can see labels, checklists, and attachments in the Kanban board and modal"
    - "Attachments render with correct Google Docs icons"
    - "Checklists show progress bars"
  artifacts:
    - path: "src/components/kanban/TaskDetailsModal.tsx"
      provides: "UI forms for managing sub-resources"
    - path: "src/components/kanban/GenericKanbanBoard.tsx"
      provides: "Kanban card display improvements"
  key_links:
    - from: "src/components/kanban/TaskDetailsModal.tsx"
      to: "src/api/tasks.ts"
      via: "React Query mutations"
---

<objective>
Implement frontend UI components for Checklists, Labels, Attachments, and Dates.

Purpose: Provide the visual interface for Kanban feature parity.
Output: Updated TaskDetailsModal and GenericKanbanBoard.
</objective>

<execution_context>
@$HOME/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-kanban-feature-parity-with-google-integrations/68-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add API Hooks</name>
  <files>src/api/tasks.ts</files>
  <read_first>src/api/tasks.ts</read_first>
  <action>
    Modify `src/api/tasks.ts`:
    1. Define `ts-rest` contracts matching the new endpoints in `shared/routes/tasks.ts`.
    2. Export React Query hooks (e.g., `useCreateChecklistMutation`, `useCreateAttachmentMutation`).
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <acceptance_criteria>
    - Hooks are available for all new sub-resource endpoints
  </acceptance_criteria>
  <done>Frontend can communicate with new backend routes</done>
</task>

<task type="auto">
  <name>Task 2: Implement TaskDetailsModal Modules</name>
  <files>src/components/kanban/TaskDetailsModal.tsx</files>
  <read_first>src/components/kanban/TaskDetailsModal.tsx</read_first>
  <action>
    Modify `src/components/kanban/TaskDetailsModal.tsx`:
    1. Add UI inputs for `startDate` and `estimatedMinutes`.
    2. Build a Checklist Module: Add a text input to append items. Render existing items with checkboxes. Show a progress bar calculated as `(completed / total) * 100`.
    3. Build an Attachments Module: Add a URL input field. Render returned attachments as rich cards using Lucide icons based on `type`.
    4. Implement Label Picker: Use standard ARES colors to allow selecting global labels.
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <acceptance_criteria>
    - All sub-resources can be viewed and mutated from the modal
  </acceptance_criteria>
  <done>Task details UI is feature complete</done>
</task>

<task type="auto">
  <name>Task 3: Enhance GenericKanbanBoard Cards</name>
  <files>src/components/kanban/GenericKanbanBoard.tsx</files>
  <read_first>src/components/kanban/GenericKanbanBoard.tsx</read_first>
  <action>
    Modify `src/components/kanban/GenericKanbanBoard.tsx`:
    1. Update the Card rendering to show Label pills at the top.
    2. Show `coverImage` if present.
    3. Show checklist progress (e.g., `2/4` icon) and attachment count icon if applicable.
    4. Enforce strict adherence to ARES color palettes for labels (`aresweb-brand-enforcement`).
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <acceptance_criteria>
    - Kanban cards reflect the new metadata visually
  </acceptance_criteria>
  <done>Board view is updated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run test` passes
</verification>

<success_criteria>
- All tasks completed
- UI renders without errors
</success_criteria>

<output>
After completion, create `.planning/phases/68-kanban-feature-parity-with-google-integrations/68-03-SUMMARY.md`
</output>
