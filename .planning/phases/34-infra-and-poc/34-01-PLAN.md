---
phase: 34
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - functions/api/routes/tba.ts
  - shared/schemas/contracts/tbaContract.ts
autonomous: true
---

# Phase 34-01: Infrastructure & POC

## Goal
Install `@hono/zod-openapi`, validate middleware compatibility, and migrate `tba.ts` as the reference implementation proving the pattern works end-to-end.

## Tasks

<task>
  <action>
    Install `@hono/zod-openapi` dependency:
    `npm install @hono/zod-openapi`
  </action>
  <read_first>
    - package.json
  </read_first>
  <acceptance_criteria>
    - `package.json` contains `@hono/zod-openapi` in dependencies
  </acceptance_criteria>
</task>

<task>
  <action>
    Refactor `shared/schemas/contracts/tbaContract.ts` to export `@hono/zod-openapi` routes instead of `ts-rest` contracts.
    
    1. Import `createRoute` from `@hono/zod-openapi` instead of `initContract`.
    2. Convert `getRankings`, `getMatches`, and `getFtcEvents` to use `createRoute({ method, path, request: { params: z.object(...) }, responses: { ... } })`.
    3. Keep the same exact Zod schemas and paths.
  </action>
  <read_first>
    - shared/schemas/contracts/tbaContract.ts
  </read_first>
  <acceptance_criteria>
    - `shared/schemas/contracts/tbaContract.ts` exports `createRoute` objects
    - The file no longer imports `@ts-rest/core`
  </acceptance_criteria>
</task>

<task>
  <action>
    Migrate `functions/api/routes/tba.ts` to use native OpenAPI routing.
    
    1. Change `const tbaRouter = new Hono<AppEnv>()` to `new OpenAPIHono<AppEnv>()`.
    2. Remove `initServer`, `s.router`, and `createHonoEndpoints`.
    3. Register endpoints using `tbaRouter.openapi(getRankingsRoute, async (c) => { ... })`.
    4. Inside the handler, read params using `const { eventKey } = c.req.valid('param')` instead of `input.params`.
    5. Return JSON responses with status codes using `return c.json({ ... }, 200)`.
    6. Ensure `tbaRouter.use("*", ensureAuth)` and `rateLimitMiddleware` remain unchanged at the top.
  </action>
  <read_first>
    - functions/api/routes/tba.ts
    - shared/schemas/contracts/tbaContract.ts
  </read_first>
  <acceptance_criteria>
    - `functions/api/routes/tba.ts` has zero `as any` or `as never` casts
    - The file no longer imports `ts-rest-hono` or `ServerInferRequest`
    - `tbaRouter` is an instance of `OpenAPIHono`
  </acceptance_criteria>
</task>

<task>
  <action>
    Validate the migration by running type checks and tests.
    `npx tsc --noEmit` and `npx vitest run functions/api/routes/tba.test.ts`
  </action>
  <read_first>
    - functions/api/routes/tba.test.ts
  </read_first>
  <acceptance_criteria>
    - `tsc --noEmit` exits with 0
    - tests pass
  </acceptance_criteria>
</task>
