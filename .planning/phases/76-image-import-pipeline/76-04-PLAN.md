---
phase: 76-image-import-pipeline
plan: 04
type: execute
wave: 4
depends_on: [76-02, 76-03]
files_modified:
  - src/api/google-photos.ts
  - src/components/dashboard/PhotoImportButton.tsx
  - src/routes/dashboard/photos.tsx
autonomous: true
requirements:
  - IMG-03
  - IMG-04
  - IMG-05
  - IMG-06
  - IMG-07
user_setup: []

must_haves:
  truths:
    - useImportPhotos mutation calls POST /import endpoint
    - Mutation accepts mediaItemIds and optional albumId
    - Import progress shows loading state during request
    - Success toast displays "Imported {count} photos" message
    - Failed imports display error details with retry option per D-20
    - Media items query invalidates after successful import
    - Individual photo failures show "Retry" button per D-19
    - Batch import continues even if some photos fail per D-21
  artifacts:
    - path: src/api/google-photos.ts
      provides: React Query hooks for import
      exports: ["useImportPhotos"]
    - path: src/components/dashboard/PhotoImportButton.tsx
      provides: Updated import button with mutation
      contains: useImportPhotos hook, error display
    - path: src/routes/dashboard/photos.tsx
      provides: Connected import flow
      contains: onImportComplete handler
  key_links:
    - from: useImportPhotos
      to: POST /import
      via: client.googlePhotos.import.$post
      pattern: "client.googlePhotos.import.*\\\$post"
    - from: PhotoImportButton
      to: useImportPhotos
      via: hook usage
      pattern: "useImportPhotos.*onSuccess"
    - from: onSuccess callback
      to: mediaItems query invalidation
      via: queryClient.invalidateQueries
      pattern: "invalidateQueries.*media"
---

<objective>
Connect import UI to backend with error handling and progress feedback.

Purpose: Create the React Query mutation that calls the import endpoint, wire it to the import button, and display appropriate feedback (loading, success, errors) to users per IMG-07.

Output: Working import flow with toast notifications, error display, retry buttons, and query invalidation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-image-import-pipeline/76-CONTEXT.md
@.planning/phases/76-image-import-pipeline/76-02-PLAN.md
@.planning/phases/76-image-import-pipeline/76-03-PLAN.md
@shared/routes/google-photos.ts
@src/api/google-photos.ts
@src/api/honoClient.ts
@src/components/dashboard/PhotoImportButton.tsx
@.agents/skills/aresweb-typescript-safety/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useImportPhotos mutation hook</name>
  <files>src/api/google-photos.ts</files>
  <action>
Add to src/api/google-photos.ts after the existing hooks:

1. Define ImportPhotosParams interface:
   - mediaItemIds: string[] - array of Google Photos media item IDs
   - albumId?: string - optional album ID if importing from album context

2. Define ImportPhotosResponse interface:
   - imported: number - count of successfully imported photos
   - failed: number - count of failed imports
   - results: Array<{ mediaItemId, status, r2Key?, error?, filename }>

3. Create useImportPhotos hook:
   - Uses useMutation from @tanstack/react-query
   - Calls client.googlePhotos.import.$post() with mediaItemIds and albumId
   - Uses unwrapResponse from honoClient
   - Includes withMutationCallbacks for:
     - onSuccess: Invalidate mediaItemsQueryKey to refresh list
     - onError: Call toastApiError with "Photo import failed" message
   - Returns mutation object with mutate, mutateAsync, isLoading, error

4. Export the hook and the interfaces

Follow existing patterns from useUploadPhotos in the same file. Use proper TypeScript types (no `as any` except at Hono boundary if needed per TypeScript Safety SKILL.md).
</action>
<verify>
<automated>grep -c "useImportPhotos" src/api/google-photos.ts && grep -c "ImportPhotosParams" src/api/google-photos.ts</automated>
</verify>
<done>useImportPhotos mutation hook created with proper TypeScript types</done>
</task>

<task type="auto">
  <name>Task 2: Update PhotoImportButton with mutation and error display</name>
  <files>src/components/dashboard/PhotoImportButton.tsx</files>
  <action>
Update PhotoImportButton to use the import mutation:

1. Add importMutation using useImportPhotos hook
2. Add onSuccess and onError callbacks to props
3. Update button onClick to call importMutation.mutate()
4. Show loading state during mutation (isLoading=true):
   - Change button text to "Importing..."
   - Disable button
   - Show spinner icon
5. On success, call onSuccess prop with response data
6. On error, display error details per D-19/D-20:
   - If results array has failures, show inline error summary
   - For each failed item, display filename and error message
   - Add "Retry Failed" button that re-runs mutation with only failed IDs
   - Use toast for error notification (via onError callback)

Error display UI:
- Below the import button, show "X imports failed:" if failed > 0
- List failed items with filenames and error messages
- Each failed item has a "Retry" button
- "Retry All Failed" button at bottom of list

Keep component accessible with proper aria labels for error announcements.
</action>
<verify>
<automated>grep -c "importMutation" src/components/dashboard/PhotoImportButton.tsx && grep -c "onSuccess" src/components/dashboard/PhotoImportButton.tsx</automated>
</verify>
<done>PhotoImportButton updated with mutation, loading state, and error display</done>
</task>

<task type="auto">
  <name>Task 3: Connect import flow in photos.tsx</name>
  <files>src/routes/dashboard/photos.tsx</files>
<action>
Update photos.tsx to handle import completion:

1. Pass onImportSuccess callback to PhotoImportButton:
   - Shows success toast: "Imported {count} photos"
   - If failed > 0, shows warning: "{count} photos failed to import"
   - Clears selectedIds Set
   - Exits select mode (setIsSelectMode(false))
   - Optionally refetches media items (though query invalidation handles this)

2. Pass onImportError callback:
   - Already handled by PhotoImportButton's internal error display
   - Can add additional error logging if needed

3. Ensure albumId is passed to import mutation when importing from album:
   - When selectedAlbumId is not null, pass it as albumId
   - This enables R2 folder structure per D-11

4. Add queryClient import if not already present for manual invalidation if needed

The import flow should work end-to-end:
- User clicks "Select Photos"
- User checks photos they want to import
- User clicks "Import Selected"
- Loading state shows during import
- On success: toast notification, selection cleared, exit select mode
- On partial failure: error details shown with retry options
- On total failure: error toast with message

Follow existing toast patterns from the codebase (check how other mutations handle success/error toasts).
</action>
<verify>
<automated>grep -c "onImportSuccess" src/routes/dashboard/photos.tsx && grep -c "selectedAlbumId" src/routes/dashboard/photos.tsx</automated>
</verify>
<done>Import flow connected in photos dashboard with success/error handling</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard → API | Import request sent via mutation |
| API → Dashboard | Success/failure response displayed to user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-14 | Information Disclosure | Error messages | mitigate | Display user-friendly errors only; detailed errors logged server-side |
| T-76-15 | Tampering | Retry requests | mitigate | Retry uses same validated IDs from original selection |

No security concerns. Error display follows existing patterns.
</threat_model>

<verification>
- [ ] useImportPhotos hook calls POST /import endpoint
- [ ] Mutation includes onSuccess callback with query invalidation
- [ ] Loading state displays correctly during import
- [ ] Success toast displays imported count
- [ ] Failed imports display inline with error messages
- [ ] Retry buttons work for individual and batch retry
- [ ] Selection clears and select mode exits after import
- [ ] Album ID passed when importing from album context
</verification>

<success_criteria>
- [ ] Import mutation created with proper TypeScript types
- [ ] Import button shows loading state during request
- [ ] Success notification displays after import
- [ ] Failed imports show error details with retry options
- [ ] Media items list refreshes after successful import
- [ ] Import from album uses correct album ID for folder structure
- [ ] End-to-end import flow works in browser
</success_criteria>

<output>
After completion, create `.planning/phases/76-image-import-pipeline/76-04-SUMMARY.md`
</output>
