---
phase: 76-image-import-pipeline
plan: 03
type: execute
wave: 3
depends_on: [76-02]
files_modified:
  - src/routes/dashboard/photos.tsx
  - src/components/dashboard/PhotoGrid.tsx
  - src/components/dashboard/PhotoImportButton.tsx
autonomous: true
requirements:
  - IMG-01
  - IMG-02
user_setup: []

must_haves:
  truths:
    - PhotoGrid displays photos from Google Photos (IMG-01 - Phase 75 completed)
    - PhotoGrid has checkbox selection mode for multi-select (IMG-02)
    - "Select Photos" button toggles checkbox mode
    - "Import Selected" button appears when photos are selected
    - Selected photo IDs tracked in component state
    - Checkboxes use ARES brand colors (ares-red for selected)
    - Selection persists across album navigation
  artifacts:
    - path: src/components/dashboard/PhotoGrid.tsx
      provides: Reusable photo grid component with selection mode
      exports: ["PhotoGrid"]
      contains: checkbox rendering, selection state, onSelectChange prop
    - path: src/routes/dashboard/photos.tsx
      provides: Updated photos dashboard with import button
      contains: "Import Selected" button, selection state
    - path: src/components/dashboard/PhotoImportButton.tsx
      provides: Import action button component
      exports: ["PhotoImportButton"]
  key_links:
    - from: PhotoGrid.tsx
      to: photos.tsx
      via: onSelectChange callback prop
      pattern: "onSelectChange.*setSelectedIds"
    - from: "Import Selected" button
      to: POST /import endpoint
      via: useImportPhotos mutation (created in 76-04)
      pattern: "importMutation.mutate.*mediaItemIds"
---

<objective>
Extend PhotoGrid with checkbox selection for batch import.

Purpose: Add multi-select capability to the existing PhotoGrid component, allowing users to select multiple photos for import. Reuse existing PhotoGrid from Phase 75 per D-01/D-02.

Output: PhotoGrid component with selectable checkboxes, "Select Photos" toggle, and "Import Selected" button.
</object>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-image-import-pipeline/76-CONTEXT.md
@.planning/phases/76-image-import-pipeline/76-02-PLAN.md
@src/routes/dashboard/photos.tsx
@.agents/skills/aresweb-brand-enforcement/SKILL.md
@.agents/skills/aresweb-web-accessibility/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PhotoImportButton component</name>
  <files>src/components/dashboard/PhotoImportButton.tsx</files>
  <action>
Create a new component PhotoImportButton that:
- Accepts props: selectedIds (string[]), disabled (boolean), onImportComplete (() => void)
- Displays "Import Selected ({count})" button text with count
- Uses ARES brand colors: bg-ares-red, hover:bg-ares-red/90
- Disabled when selectedIds is empty or disabled=true
- Includes loading state with spinner during import
- Triggers import mutation (will be implemented in 76-04)
- For now, the component is a shell that accepts an onImport callback prop

The component should be accessible:
- Button has proper aria-label for screen readers
- Loading state announced via aria-live
- Disabled state properly indicated

Follow ARES brand guidelines from SKILL.md.
</action>
<verify>
<automated>grep -c "PhotoImportButton" src/components/dashboard/PhotoImportButton.tsx && grep -c "ares-red" src/components/dashboard/PhotoImportButton.tsx</automated>
</verify>
<done>PhotoImportButton component created with proper styling and accessibility</done>
</task>

<task type="auto">
  <name>Task 2: Extract PhotoGrid from photos.tsx with selection props</name>
  <files>src/components/dashboard/PhotoGrid.tsx, src/routes/dashboard/photos.tsx</files>
  <action>
Extract the photo grid rendering from photos.tsx into a new reusable PhotoGrid component at src/components/dashboard/PhotoGrid.tsx.

PhotoGrid props:
- mediaItems: Array of photo objects (id, filename, baseUrl, etc.)
- selectable: boolean (default false) - enables checkbox mode
- selectedIds: Set<string> or string[] - currently selected IDs
- onSelectChange: (id: string, selected: boolean) => void - callback when checkbox toggled
- onSelectAll: (selected: boolean) => void - callback for select all toggle (optional)

Component behavior:
- When selectable=false: renders photos as-is (existing Phase 75 behavior)
- When selectable=true: adds checkbox overlay on each photo
- Checkbox styling:
  - Unchecked: transparent border, white/ares-bronze checkbox icon
  - Checked: bg-ares-red, white checkmark icon
  - Position: top-right corner of photo, z-index above image
- Photo card gets border-2 border-ares-red when selected
- Clicking anywhere on photo toggles selection (in selectable mode)

Accessibility:
- Checkbox has proper label: "Select {filename}"
- Space/Enter toggles checkbox when focused
- Select all functionality available (optional prop)

Keep existing hover effects and responsive grid layout from Phase 75.
</action>
<verify>
<automated>grep -c "export.*PhotoGrid" src/components/dashboard/PhotoGrid.tsx && grep -c "selectable" src/components/dashboard/PhotoGrid.tsx</automated>
</verify>
<done>PhotoGrid component extracted with selection mode support</done>
</task>

<task type="auto">
  <name>Task 3: Update photos.tsx with selection state and import button</name>
  <files>src/routes/dashboard/photos.tsx</files>
  <action>
Update the photos dashboard to add selection functionality:

1. Add state for selection mode:
   - const [isSelectMode, setIsSelectMode] = useState(false)
   - const [selectedIds, setSelectedIds] = useState<Set<string>>(new Set())

2. Add "Select Photos" toggle button in header (next to Upload button):
   - Shows "Select Photos" when mode is off
   - Shows "Cancel Selection" when mode is on
   - Toggles isSelectMode state
   - Styled as outline button (border border-ares-bronze/30)

3. When isSelectMode is true:
   - Pass selectable=true to PhotoGrid
   - Show "Import Selected ({count})" button using PhotoImportButton
   - PhotoImportButton disabled when selectedIds.size === 0

4. Implement selection handlers:
   - handleSelectPhoto(id, selected): updates selectedIds Set
   - handleSelectAll(selected): selects/deselects all visible photos
   - Clear selectedIds when switching albums or exiting select mode

5. Import button (PhotoImportButton):
   - Pass selectedIds (as Array.from(selectedIds))
   - For now, onImportComplete just clears selection and exits select mode
   - The actual import mutation will be added in 76-04

Maintain existing UI layout and styling. Follow ARES brand colors.
</action>
<verify>
<automated>grep -c "isSelectMode" src/routes/dashboard/photos.tsx && grep -c "selectedIds" src/routes/dashboard/photos.tsx && grep -c "PhotoImportButton" src/routes/dashboard/photos.tsx</automated>
</verify>
<done>Photos dashboard updated with selection mode and import button</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → PhotoGrid | Checkbox selection is client-side only |
| PhotoGrid → photos.tsx | Selected IDs passed to parent for import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-12 | Spoofing | Selected IDs | mitigate | IDs are from Google Photos API response, not user input; re-validated server-side |
| T-76-13 | Tampering | Selection state | accept | Client-side state only; server validates all IDs before import |

No security concerns for UI-only selection logic. Server-side validation in 76-02 protects against malicious input.
</threat_model>

<verification>
- [ ] PhotoGrid component is reusable and accepts selection props
- [ ] Checkboxes display correctly in selectable mode
- [ ] Selection state properly managed with Set data structure
- [ ] Import button appears only when photos are selected
- [ ] ARES brand colors applied correctly (ares-red, ares-bronze)
- [ ] Accessibility requirements met (labels, keyboard nav)
- [ ] Selection clears when switching albums
</verification>

<success_criteria>
- [ ] PhotoGrid component extracted from photos.tsx
- [ ] Checkboxes appear in select mode with proper styling
- [ ] "Select Photos" toggle button works
- [ ] "Import Selected" button shows count and appears/disappears based on selection
- [ ] Multiple photos can be selected/deselected
- [ ] Selection state persists during pagination
- [ ] TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/76-image-import-pipeline/76-03-SUMMARY.md`
</output>
