---
phase: 76-image-import-pipeline
plan: 02
type: tdd
wave: 2
depends_on: [76-01]
files_modified:
  - functions/api/routes/google-photos/index.ts
  - functions/api/routes/google-photos/index.test.ts
  - functions/utils/imageImport.ts
  - functions/utils/imageImport.test.ts
autonomous: true
requirements:
  - IMG-03
  - IMG-04
  - IMG-05
  - IMG-06
  - IMG-07
  - ALBUMS-01
  - ALBUMS-02
  - ALBUMS-03
user_setup: []

must_haves:
  truths:
    - POST /import endpoint downloads photos from Google Photos API
    - Images validated using magic bytes (JPG, PNG, WEBP per D-07)
    - Valid images uploaded to R2 with proper metadata
    - Album metadata stored in D1 on first import from album
    - Import audit log records all attempts (success and failure)
    - Sequential processing prevents memory overflow per D-06
    - Individual failures don't stop batch processing per D-21
  artifacts:
    - path: functions/utils/imageImport.ts
      provides: Image import utilities (download, validation, R2 upload)
      exports: ["validateImageMagicBytes", "downloadPhoto", "uploadToR2", "sanitizeAlbumName"]
    - path: functions/api/routes/google-photos/index.ts
      provides: POST /import endpoint handler
      exports: ["photosRouter"]
    - path: functions/api/routes/google-photos/index.test.ts
      provides: TDD tests for import endpoint
    - path: functions/utils/imageImport.test.ts
      provides: TDD tests for import utilities
  key_links:
    - from: "POST /import handler"
      to: "validateImageMagicBytes"
      via: direct function call
      pattern: "validateImageMagicBytes.*buffer"
    - from: "POST /import handler"
      to: "uploadToR2"
      via: direct function call
      pattern: "uploadToR2.*env.ARES_STORAGE"
    - from: "POST /import handler"
      to: "D1 imported_photos table"
      via: db.insert()
      pattern: "db.insert.*imported_photos"
    - from: "POST /import handler"
      to: "Google Photos API"
      via: fetch with baseUrl=d
      pattern: "fetch.*baseUrl.*=d"
---

<objective>
Implement photo import pipeline with TDD.

Purpose: Download photos from Google Photos API, validate them, upload to R2, and track imports in D1. Uses Test-Driven Development: write failing tests first, then implement to make tests pass.

Output: Working POST /import endpoint with image validation, R2 upload, album metadata tracking, and comprehensive test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-image-import-pipeline/76-CONTEXT.md
@.planning/phases/76-image-import-pipeline/76-01-PLAN.md
@shared/routes/google-photos.ts
@functions/api/routes/google-photos/index.ts
@functions/api/routes/media/index.ts
@functions/utils/googleAuth.ts
@src/db/schema.ts
@.agents/skills/aresweb-typescript-safety/SKILL.md
</context>

<feature>
  <name>Photo Import Pipeline</name>
  <files>functions/utils/imageImport.ts, functions/utils/imageImport.test.ts, functions/api/routes/google-photos/index.ts, functions/api/routes/google-photos/index.test.ts</files>
  <behavior>
    Test 1: validateImageMagicBytes returns {valid:true, format:"jpg"} for JPG magic bytes (FF D8 FF)
    Test 2: validateImageMagicBytes returns {valid:true, format:"png"} for PNG magic bytes (89 50 4E 47)
    Test 3: validateImageMagicBytes returns {valid:true, format:"webp"} for WEBP magic bytes (52 49 46 46)
    Test 4: validateImageMagicBytes returns {valid:false, format:"unknown"} for invalid magic bytes
    Test 5: validateImageMagicBytes rejects files larger than 50MB (configurable via env var)
    Test 6: downloadPhoto fetches from baseUrl with =d suffix for full resolution
    Test 7: downloadPhoto throws ApiError on fetch failure
    Test 8: uploadToR2 puts file to ARES_STORAGE with correct key format
    Test 9: uploadToR2 returns R2 key on success
    Test 10: sanitizeAlbumName lowercases, replaces spaces with hyphens, removes special chars
    Test 11: POST /import with valid mediaItemIds returns 200 with import counts
    Test 12: POST /import with valid mediaItemIds downloads and uploads to R2
    Test 13: POST /import creates record in imported_photos table on success
    Test 14: POST /import creates record in import_audit_log for each attempt
    Test 15: POST /import with albumId creates/updates photo_albums record
    Test 16: POST /import with albumId uploads to photos/albums/{sanitizedName}/{filename}
    Test 17: POST /import without albumId uploads to photos/imported/{YYYY-MM-DD}/{filename}
    Test 18: POST /import continues processing after individual failures
    Test 19: POST /import returns failure details for each failed import
    Test 20: POST /import validates magic bytes before R2 upload
  </behavior>
  <implementation>
    RED phase: Write failing tests in imageImport.test.ts and google-photos/index.test.ts for all 20 behaviors.

    GREEN phase: Implement imageImport.ts utilities:
    - validateImageMagicBytes(buffer: ArrayBuffer, maxSize: number): checks first 4-12 bytes for JPG/PNG/WEBP signatures, returns {valid, format}
    - downloadPhoto(baseUrl: string, token: string): fetches with =d suffix, returns ArrayBuffer
    - uploadToR2(buffer: ArrayBuffer, filename: string, mimeType: string, albumName: string | null, env: AppEnv["env"], date: string): uploads to ARES_STORAGE with key format per D-10/D-11
    - sanitizeAlbumName(name: string): lowercase, hyphens for spaces, remove non-alphanumeric/hyphen

    Implement POST /import in google-photos/index.ts:
    - Parse mediaItemIds and optional albumId from request body
    - Get Photos access token via getPhotosAccessToken
    - Fetch media item details from Photos API to get baseUrl for each ID
    - Process sequentially: download, validate, upload, record
    - On validation failure, record audit log with failure, continue to next
    - On success, insert imported_photos record and audit log
    - If albumId provided, sync album metadata to photo_albums table
    - Return {imported, failed, results: [...]}

    REFACTOR phase: Extract common patterns, optimize if needed, ensure all tests pass.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → Google Photos API | OAuth token used to download photos |
| Worker → R2 | Validated images uploaded |
| Worker → D1 | Import records written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-07 | Tampering | Image validation | mitigate | Magic byte validation prevents malformed files; size limit prevents DoS |
| T-76-08 | Information Disclosure | Error messages | mitigate | Generic errors to client; detailed errors in server logs only |
| T-76-09 | Denial of Service | Sequential processing | mitigate | Process items one-at-a-time per D-06; limit batch size in validation |
| T-76-10 | Spoofing | Album ID | mitigate | Fetch album details from Google Photos API, don't trust user input |
| T-76-11 | Tampering | Filenames | mitigate | Sanitize filenames; use generated R2 keys not user-provided paths |

No critical vulnerabilities. Magic byte validation prevents file type spoofing attacks.
</threat_model>

<verification>
- [ ] All 20 tests fail initially (RED phase)
- [ ] All 20 tests pass after implementation (GREEN phase)
- [ ] Magic byte validation correctly identifies JPG, PNG, WEBP
- [ ] R2 key format matches D-10/D-11 (photos/imported/{date}/ or photos/albums/{name}/)
- [ ] Album name sanitization produces safe folder names
- [ ] Audit log records both successes and failures
- [ ] Sequential processing verified in tests (no parallel operations)
</verification>

<success_criteria>
- [ ] imageImport.ts utilities implemented and tested
- [ ] POST /import endpoint implemented in google-photos/index.ts
- [ ] All TDD tests pass (100% coverage of import behavior)
- [ ] R2 uploads succeed with correct key format
- [ ] D1 records created for all import attempts
- [ ] Album metadata synced on first album import
</success_criteria>

<output>
After completion, create `.planning/phases/76-image-import-pipeline/76-02-SUMMARY.md`

Commits:
- test(76-02): add failing tests for photo import pipeline
- feat(76-02): implement photo import utilities and endpoint
- refactor(76-02): clean up import implementation
</output>
