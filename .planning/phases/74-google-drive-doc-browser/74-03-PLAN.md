---
phase: 74-google-drive-doc-browser
plan: 03
type: execute
wave: 3
depends_on: ["74-01", "74-02"]
files_modified: ["src/api/google-drive.ts"]
autonomous: true
requirements: ["DOCS-01", "DOCS-02", "DOCS-03", "DOCS-04", "DOCS-05", "DOCS-06"]
user_setup: []
must_haves:
  truths:
    - "Frontend can call GET /files endpoint via typed Hono client"
    - "Query parameters (q, pageToken, pageSize) are type-safe"
    - "Response type matches driveFileSchema from contract"
    - "Error handling follows ARESWEB patterns"
  artifacts:
    - path: "src/api/google-drive.ts"
      provides: "Typed React Query hooks for Google Drive API"
      exports: ["useGetDriveFiles", "useGetDriveFilesQuery"]
      min_lines: 40
  key_links:
    - from: "src/api/google-drive.ts"
      to: "shared/routes/google-drive.ts"
      via: "hc client type inference"
      pattern: "client.googleDrive.files"
    - from: "src/api/google-drive.ts"
      to: "src/api/honoClient.ts"
      via: "unwrapResponse utility"
      pattern: "unwrapResponse"
---

<objective>
Create type-safe React Query hooks for Google Drive files endpoint, enabling frontend consumption with full TypeScript inference.

Purpose: Provide the frontend API layer that connects the dashboard UI to the backend Drive files endpoint. Following the established pattern from `src/api/youtube.ts`, these hooks give developers autocomplete, type checking, and error handling out of the box.

Output: API client file with React Query hooks for listing Drive files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-google-drive-doc-browser/74-CONTEXT.md
@shared/routes/google-drive.ts
@src/api/honoClient.ts
@src/api/youtube.ts
@src/api/videos.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/api/google-drive.ts with React Query hooks</name>
  <files>src/api/google-drive.ts</files>
  <action>
Create new file `src/api/google-drive.ts` following the pattern in `src/api/youtube.ts`:

1. Import useQuery from "@tanstack/react-query"
2. Import client and unwrapResponse from "./honoClient"
3. Import route types (inferred via client, no direct import needed)

4. Create `useGetDriveFiles` hook:
   - Takes optional params: { q?: string; pageToken?: string; pageSize?: number }
   - Query key: ["google-drive", "files", params]
   - Query function: calls client.googleDrive.files.$get({ query: params })
   - Returns unwrapped response with type inference from route contract
   - Response type includes: { files: DriveFile[], nextPageToken?: string }

5. Export the hook

Follow TypeScript Safety SKILL.md:
- No `as any` except at c.json boundary (server-side only)
- Infer types from route contract, not hand-written interfaces
- Use unwrapResponse for consistent error handling

DO NOT create mutations (read-only endpoint in Phase 74).
</action>
  <verify>File exists with useGetDriveFiles hook exported. grep -c "useGetDriveFiles" src/api/google-drive.ts equals 1</verify>
  <done>Type-safe React Query hook created. Frontend can fetch Drive files with full type inference and automatic error handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Client-side hooks call protected endpoint |
| React Query Cache | Client-side caching of API responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-11 | T | Unauthenticated client request | mitigate | Endpoint requires ensureAdmin middleware, unauthenticated requests return 401 |
| T-74-12 | I | Cache poisoning | accept | React Query cache is client-side, cleared on refresh, no sensitive data (file metadata only) |
| T-74-13 | R | Replay attack | accept | Read-only data fetch, no state changes |
</threat_model>

<verification>
1. File src/api/google-drive.ts exists
2. useGetDriveFiles hook exported
3. Hook calls client.googleDrive.files.$get()
4. Query key includes all parameters for proper cache invalidation
5. TypeScript compilation succeeds with inferred types
</verification>

<success_criteria>
- Frontend can fetch Google Drive files via useGetDriveFiles hook
- Query parameters (q, pageToken, pageSize) are type-safe
- Response type matches driveFileSchema from route contract
- Error handling via unwrapResponse follows ARESWEB patterns
</success_criteria>

<output>
After completion, create `.planning/phases/74-google-drive-doc-browser/74-03-SUMMARY.md`
</output>
