---
phase: 74-google-drive-doc-browser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["shared/routes/google-drive.ts"]
autonomous: true
requirements: ["DOCS-01", "DOCS-02", "DOCS-03", "DOCS-04", "DOCS-05", "DOCS-06"]
user_setup: []
must_haves:
  truths:
    - "Google Drive files route contract defines file listing query parameters"
    - "Response schema includes Google Workspace file metadata (id, name, mimeType, modifiedTime, owner)"
    - "Search query parameter supports name filtering per DOCS-02"
    - "Pagination supported via pageToken query parameter"
    - "Document type enum matches Google Workspace MIME types per D-03"
  artifacts:
    - path: "shared/routes/google-drive.ts"
      provides: "OpenAPI route contracts for Google Drive files endpoint"
      exports: ["listDriveFilesRoute", "driveFileSchema", "listDriveFilesQuerySchema"]
      min_lines: 50
  key_links:
    - from: "shared/routes/google-drive.ts"
      to: "functions/api/routes/google-drive/index.ts"
      via: "route contract import"
      pattern: "listDriveFilesRoute"
---

<objective>
Define OpenAPI/Zod contracts for Google Drive file listing endpoint with search, pagination, and Google Workspace MIME type filtering.

Purpose: Create type-safe route contracts that enable end-to-end type inference from server handler through Hono client to frontend usage. This interface-first approach prevents the scavenger hunt anti-pattern where executors explore the codebase to understand expected shapes.

Output: Route contract file with query schema, response schema, and individual file schema defining Google Workspace document metadata structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-google-drive-doc-browser/74-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK_GOOGLE_DRIVE.md
@.planning/REQUIREMENTS.md
@shared/routes/common.ts
@shared/routes/youtube.ts
@functions/api/routes/google-drive/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared/routes/google-drive.ts with OpenAPI contracts</name>
  <files>shared/routes/google-drive.ts</files>
  <action>
Create new file `shared/routes/google-drive.ts` following the pattern in `shared/routes/youtube.ts`:

1. Import zod and createRoute from "@hono/zod-openapi"
2. Import standardErrors from "./common"

3. Define `driveFileSchema` with z.object containing:
   - id: z.string().openapi({ example: "1AbcDefGhiJklmnoPqrsTuvWxYz" })
   - name: z.string().openapi({ example: "Team Meeting Notes" })
   - mimeType: z.string().openapi({ example: "application/vnd.google-apps.document" })
   - modifiedTime: z.string().datetime().openapi({ example: "2024-05-12T10:30:00.000Z" })
   - owner: z.string().optional().openapi({ example: "mentor@aresfirst.org" })
   - webViewLink: z.string().url().optional().openapi({ example: "https://docs.google.com/document/d/1AbcDef/edit" })

4. Define `documentType` enum discriminating Google Workspace types per D-03:
   - "document" (Docs)
   - "spreadsheet" (Sheets)
   - "presentation" (Slides)
   - "drawing" (Drawings)

5. Define `listDriveFilesQuerySchema` with z.object containing:
   - q: z.string().optional().openapi({ description: "Search query - filters by name contains" })
   - pageToken: z.string().optional().openapi({ description: "Pagination token from previous response" })
   - pageSize: z.coerce.number().min(1).max(100).default(50).optional()

6. Define `listDriveFilesResponseSchema` with z.object containing:
   - files: z.array(driveFileSchema)
   - nextPageToken: z.string().optional()

7. Define `listDriveFilesRoute` using createRoute with:
   - method: "get"
   - path: "/files"
   - request.query: listDriveFilesQuerySchema
   - responses: 200 with listDriveFilesResponseSchema, plus standardErrors
   - tags: ["google-drive", "admin"]

DO NOT use @ts-ignore or any type assertions. Follow TypeScript Safety SKILL.md patterns.
</action>
  <verify>File exists with exported route contract and schemas. grep -c "listDriveFilesRoute" shared/routes/google-drive.ts equals 1</verify>
  <done>OpenAPI route contract exported. Frontend can infer query parameter types. Response schema defines Drive file metadata structure including Google Workspace MIME types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted client input (query params) crosses here |
| API → Google Drive API | Service account token authenticates outbound requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | T | Client → API (q parameter) | mitigate | Zod schema validates query, pageSize bounded to 1-100, q parameter limited to string type with no SQL injection risk (passed to Drive API, not D1) |
| T-74-02 | I | Response data from Drive API | accept | Read-only data, no PII beyond file metadata already visible to service account |
| T-74-03 | D | D1 unavailable for token refresh | accept | getDriveAccessToken handles missing tokens with ApiError, no D1 access in this plan (data fetching only) |
| T-74-04 | E | Google Drive API rate limit | accept | Read-only listing with 50-item default pageSize, low volume per D-13 (no caching in Phase 74) |
| T-74-05 | R | API response replay | accept | Read-only file listing, no state changes, replay safe |
</threat_model>

<verification>
1. Route contract file exists at shared/routes/google-drive.ts
2. listDriveFilesRoute exported with proper OpenAPI metadata
3. driveFileSchema defines all required fields per DOCS-03 (name, type, modifiedTime, owner)
4. Query schema supports search (q) and pagination (pageToken, pageSize) per D-04/D-12
5. TypeScript compilation succeeds with zero errors
</verification>

<success_criteria>
- OpenAPI contract defines file listing endpoint with query parameters for search and pagination
- Response schema includes all metadata fields required by DOCS-03
- File type enum represents Google Workspace MIME types per D-03
- Contract ready for use by backend handler (74-02) and frontend client (74-03)
</success_criteria>

<output>
After completion, create `.planning/phases/74-google-drive-doc-browser/74-01-SUMMARY.md`
</output>
