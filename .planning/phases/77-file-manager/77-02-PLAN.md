---
phase: 77-file-manager
plan: 02
type: tdd
wave: 2
depends_on: [77-01]
files_modified:
  - functions/api/routes/files/index.ts
  - functions/api/routes/files/index.test.ts
  - functions/utils/fileValidation.ts
  - functions/utils/fileValidation.test.ts
autonomous: true
requirements:
  - FILES-01
  - FILES-02
  - FILES-03
  - FILES-04
  - FILES-06
  - FILES-08
user_setup: []

must_haves:
  truths:
    - POST /upload validates file MIME types against allowlist
    - POST /upload validates file size limit (25MB default)
    - POST /upload sanitizes filenames and uploads to R2
    - POST /upload creates record in uploaded_files table
    - POST /upload generates UUID for public download URL
    - GET / returns list of uploaded files with usage counts
    - GET /download/:id serves file from R2 with authentication
    - DELETE /:id deletes file from R2 and D1 record
    - POST /import-from-drive downloads from Drive and uploads to R2
    - POST /scan-usage scans posts content for file references
  artifacts:
    - path: functions/utils/fileValidation.ts
      provides: File validation utilities
      exports: ["validateDocumentFile", "sanitizeFilename", "getMimeTypeIcon"]
    - path: functions/api/routes/files/index.ts
      provides: File management API endpoints
      exports: ["filesRouter"]
    - path: functions/api/routes/files/index.test.ts
      provides: TDD tests for file endpoints
    - path: functions/utils/fileValidation.test.ts
      provides: TDD tests for validation utilities
  key_links:
    - from: "POST /upload handler"
      to: "validateDocumentFile"
      via: direct function call
      pattern: "validateDocumentFile.*file"
    - from: "POST /upload handler"
      to: "R2 ARES_STORAGE"
      via: env.ARES_STORAGE.put()
      pattern: "env.ARES_STORAGE.put.*documents"
    - from: "POST /upload handler"
      to: "uploaded_files table"
      via: db.insert()
      pattern: "db.insert.*uploaded_files"
    - from: "POST /import-from-drive handler"
      to: "Google Drive API"
      via: fetch with alt=media
      pattern: "fetch.*drive.*alt=media"
---

<objective>
Implement file upload, download, management, and Drive import endpoints with TDD.

Purpose: Handle document file uploads, Google Drive imports, file management, and usage tracking. Uses Test-Driven Development: write failing tests first, then implement to make tests pass.

Output: Working file management API with validation, R2 integration, and comprehensive test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-file-manager/77-CONTEXT.md
@.planning/phases/77-file-manager/77-01-PLAN.md
@shared/routes/files.ts
@functions/api/routes/media/index.ts
@functions/api/routes/google-drive/index.ts
@functions/utils/googleAuth.ts
@functions/utils/imageImport.ts
@src/db/schema.ts
@.agents/skills/aresweb-typescript-safety/SKILL.md
</context>

<feature>
  <name>File Management Pipeline</name>
  <files>functions/utils/fileValidation.ts, functions/utils/fileValidation.test.ts, functions/api/routes/files/index.ts, functions/api/routes/files/index.test.ts</files>
  <behavior>
    Test 1: validateDocumentFile returns {valid:true, mimeType:"application/pdf"} for PDF magic bytes (25 50 44 46)
    Test 2: validateDocumentFile returns {valid:true, mimeType:"application/vnd.openxmlformats-officedocument.wordprocessingml.document"} for DOCX magic bytes (50 4B 03 04)
    Test 3: validateDocumentFile returns {valid:true, mimeType:"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"} for XLSX magic bytes (50 4B 03 04 with different content type)
    Test 4: validateDocumentFile returns {valid:true, mimeType:"application/vnd.openxmlformats-officedocument.presentationml.presentation"} for PPTX magic bytes (50 4B 03 04 with different content type)
    Test 5: validateDocumentFile returns {valid:true, mimeType:"text/plain"} for TXT files
    Test 6: validateDocumentFile returns {valid:false, error:"invalid_type"} for disallowed MIME types
    Test 7: validateDocumentFile returns {valid:false, error:"file_too_large"} when size exceeds MAX_FILE_SIZE_MB * 1024 * 1024
    Test 8: sanitizeFilename removes special characters, keeps extension
    Test 9: sanitizeFilename lowercases, replaces spaces with hyphens
    Test 10: getMimeTypeIcon returns icon name for PDF (FileText), DOCX (FileText), XLSX (Table), PPTX (Presentation), TXT (File)
    Test 11: POST /upload with valid PDF file returns 200 with uploadedFile record
    Test 12: POST /upload creates record in uploaded_files table with correct r2Key format (documents/{YYYY-MM-DD}/{filename})
    Test 13: POST /upload rejects file larger than MAX_FILE_SIZE_MB with 400 error
    Test 14: POST /upload rejects disallowed MIME type with 400 error
    Test 15: POST /upload generates UUID for file ID
    Test 16: GET / returns list of files with usageCount from file_usage table
    Test 17: GET /search filters files by filename (contains query)
    Test 18: GET /download/:id returns file from R2 with correct Content-Type
    Test 19: GET /download/:id returns 404 for non-existent file ID
    Test 20: GET /download/:id requires authentication (returns 401 without auth)
    Test 21: DELETE /:id deletes file from R2 and D1 record
    Test 22: DELETE /:id returns 404 for non-existent file ID
    Test 23: POST /import-from-drive downloads file from Google Drive API
    Test 24: POST /import-from-drive validates downloaded file before R2 upload
    Test 25: POST /import-from-drive sets source="google-drive" in uploaded_files record
    Test 26: POST /import-from-drive uses getDriveAccessToken for authentication
    Test 27: POST /scan-usage scans posts.contentDraft and posts.ast for /api/files/download/{id} patterns
    Test 28: POST /scan-usage creates/updates file_usage records for found references
    Test 29: POST /scan-usage returns count of scanned posts and updated usage records
  </behavior>
  <implementation>
    RED phase: Write failing tests in fileValidation.test.ts and files/index.test.ts for all 29 behaviors.

    GREEN phase: Implement fileValidation.ts utilities:
    - validateDocumentFile(file: File, maxSize: number): checks magic bytes for PDF/DOCX/XLSX/PPTX/TXT, returns {valid, mimeType, error?}
    - sanitizeFilename(name: string): removes special chars, replaces spaces with hyphens, keeps extension
    - getMimeTypeIcon(mimeType: string): returns icon component name for UI

    Implement files router in functions/api/routes/files/index.ts:
    - POST /upload: Parse FormData, validate file, upload to R2 with key documents/{YYYY-MM-DD}/{sanitizedFilename}, create D1 record with UUID, return uploadedFile
    - GET /: Query uploaded_files with optional search filter, join with file_usage for counts, return files list
    - GET /download/:id: Query uploaded_files by id, fetch from R2, serve with correct Content-Type, require auth
    - DELETE /:id: Query uploaded_files by id, delete from R2 and D1, return success
    - POST /import-from-drive: Get Drive token, download file with ?alt=media, validate, upload to R2, create D1 record with source="google-drive"
    - POST /scan-usage: Query all posts, scan contentDraft and AST for /api/files/download/{id} regex, update file_usage table

    R2 key format per D-04: documents/{YYYY-MM-DD}/{sanitizedFilename}
    Magic byte validation patterns:
    - PDF: 25 50 44 46 (%PDF)
    - DOCX/XLSX/PPTX (Office Open XML): 50 4B 03 04 (PK header), check [Content_Types].xml for specific type
    - TXT: No magic bytes, validate by MIME type only

    REFACTOR phase: Extract common patterns, optimize if needed, ensure all tests pass.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → R2 | Validated files uploaded |
| Worker → D1 | File metadata and usage records written |
| Worker → Google Drive API | OAuth token used to download files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-07 | Tampering | File validation | mitigate | Magic byte validation prevents MIME spoofing; size limit prevents DoS |
| T-77-08 | Spoofing | Download endpoint | mitigate | Require authentication per D-24; validate file ID exists before serving |
| T-77-09 | Information Disclosure | Error messages | mitigate | Generic errors to client; detailed errors in server logs only |
| T-77-10 | Denial of Service | Import from Drive | mitigate | Sequential processing per Phase 76 pattern; validate file size before download |
| T-77-11 | Spoofing | File ID in URLs | mitigate | Use UUID format, validate against D1 before serving |
| T-77-12 | Injection | Filename sanitization | mitigate | Remove special chars, use only alphanumeric, hyphens, underscores, and single dot for extension |

No critical vulnerabilities. Magic byte validation prevents file type spoofing. Authentication required for downloads.
</threat_model>

<verification>
- [ ] All 29 tests fail initially (RED phase)
- [ ] All 29 tests pass after implementation (GREEN phase)
- [ ] Magic byte validation correctly identifies PDF, DOCX, XLSX, PPTX
- [ ] R2 key format matches D-04 (documents/{YYYY-MM-DD}/{filename})
- [ ] Filename sanitization produces safe filenames
- [ ] Authentication required for download endpoint
- [ ] Drive import uses service account token
- [ ] Usage scanning correctly extracts file IDs from post content
</verification>

<success_criteria>
- [ ] fileValidation.ts utilities implemented and tested
- [ ] File management endpoints implemented in files/index.ts
- [ ] All TDD tests pass (100% coverage of file management behavior)
- [ ] R2 uploads succeed with correct key format
- [ ] D1 records created for all uploads
- [ ] Download endpoint requires authentication
- [ ] Drive import downloads and uploads to R2
- [ ] Usage tracking scans posts correctly
</success_criteria>

<output>
After completion, create `.planning/phases/77-file-manager/77-02-SUMMARY.md`

Commits:
- test(77-02): add failing tests for file management pipeline
- feat(77-02): implement file validation and management endpoints
- refactor(77-02): clean up file management implementation
</output>
