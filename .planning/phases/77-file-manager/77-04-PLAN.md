---
phase: 77-file-manager
plan: 04
type: execute
wave: 4
depends_on: [77-03]
files_modified:
  - src/routes/dashboard/files.tsx
  - src/components/FileList.tsx
  - src/components/FileUploadZone.tsx
  - src/components/FileBrowserModal.tsx
autonomous: true
requirements: ["FILES-01", "FILES-02", "FILES-05", "FILES-07"]
user_setup: []
must_haves:
  truths:
    - "User can visit /dashboard/files to manage uploaded files"
    - "File list displays Name, Type, Size, Uploaded, Actions columns"
    - "User can upload files via drag-drop zone"
    - "User can import files from Google Drive with 'Import to Files' button"
    - "User can search files by filename"
    - "User can download, delete, and copy link for each file"
    - "Usage count displays for files linked in posts"
    - "File browser modal available for blog editor integration"
  artifacts:
    - path: src/routes/dashboard/files.tsx
      provides: "Dashboard file management page"
      min_lines: 100
    - path: src/components/FileList.tsx
      provides: "File list table component"
      min_lines: 80
    - path: src/components/FileUploadZone.tsx
      provides: "Drag-drop file upload component"
      min_lines: 60
    - path: src/components/FileBrowserModal.tsx
      provides: "File selection modal for blog editor"
      min_lines: 100
  key_links:
    - from: "src/routes/dashboard/files.tsx"
      to: "src/hooks/useFiles.ts"
      via: "React Query hooks import"
      pattern: "useFilesQuery|useUploadMutation"
    - from: "src/components/FileBrowserModal.tsx"
      to: "src/components/BlogEditor.tsx"
      via: "modal import and file link insertion"
      pattern: "FileBrowserModal"
    - from: "src/components/FileList.tsx"
      to: "shared/routes/files.ts"
      via: "type import for UploadedFile"
      pattern: "UploadedFile|uploadedFile"
---

<objective>
Build dashboard file management UI with upload, Drive import, and blog editor integration.

Purpose: Provide user interface for manual file uploads, Google Drive imports, file management (view, search, delete), and blog editor integration via file browser modal.

Output: Dashboard page at /dashboard/files with file list, upload zone, Drive import integration, and reusable file browser modal for blog editor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-file-manager/77-CONTEXT.md
@.planning/phases/77-file-manager/77-03-PLAN.md
@shared/routes/files.ts
@src/hooks/useFiles.ts
@src/routes/dashboard/drive-docs.tsx
@src/components/AssetPickerModal.tsx
@src/components/BlogEditor.tsx
@src/components/editor/RichEditorToolbar.tsx
@.agents/skills/aresweb-brand-enforcement/SKILL.md
@.agents/skills/aresweb-web-accessibility/SKILL.md
@.agents/skills/aresweb-typescript-safety/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/components/FileUploadZone.tsx component</name>
  <files>src/components/FileUploadZone.tsx</files>
  <action>
Create new component following ARES brand patterns:

Props interface:
- onUpload: (file: File, title?: string, description?: string) => void
- isUploading: boolean
- acceptedFormats: string (default: "PDF, DOCX, XLSX, PPTX, TXT")

UI elements:
- Dashed border zone with ARES-gold accent (use ares-cut-sm)
- Drag-and-drop events (onDragOver, onDrop)
- File input trigger (hidden input, button to open)
- File type hint: "Max 25MB. PDF, DOCX, XLSX, PPTX, TXT"
- Progress indicator during upload (Loader2 from lucide-react)
- Upload button (UploadCloud icon)

Brand compliance (SKILL.md):
- Use ares-red for errors, ares-gold for accent
- Use League Spartan for headings, Inter for body text
- Use ares-cut-sm for cut corners

Accessibility (SKILL.md):
- keyboard-navigable upload button (tabindex, focus-visible ring)
- aria-label for drag zone
- aria-live for upload status updates

DO NOT use @ts-ignore or any type assertions.
</action>
  <verify>File exists with exported component. grep -c "export.*function.*FileUploadZone\|export default" src/components/FileUploadZone.tsx equals 1</verify>
  <done>FileUploadZone component with drag-drop, keyboard navigation, and ARES branding.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/components/FileList.tsx component</name>
  <files>src/components/FileList.tsx</files>
  <action>
Create new component for file list table:

Props interface:
- files: UploadedFile[] (from shared/routes/files.ts)
- onDownload: (file: UploadedFile) => void
- onDelete: (id: string) => void
- onCopyLink: (url: string) => void
- isLoading: boolean
- searchQuery: string

UI elements:
- Table with columns: Name (with icon), Type, Size, Uploaded, Actions
- File type icons: FileText (PDF/DOCX), Table (XLSX), Presentation (PPTX), File (TXT)
- Size formatting: KB/MB
- Date formatting: YYYY-MM-DD HH:MM
- Actions column: Download (Download icon), Delete (Trash2 icon), Copy Link (Copy icon)
- Search filter by filename (client-side for Phase 77 per D-18)
- Empty state: "No files uploaded yet" with FileUploa icon
- Loading skeleton during fetch

Brand compliance:
- Table header: uppercase tracking-widest text-xs font-bold text-marble/60
- Hover rows: bg-white/5
- Action buttons: ares-cyan for download, ares-red for delete

Accessibility:
- Table headers with scope="col"
- Row actions accessible via keyboard
- aria-label for action buttons
- Sortable columns (name, date, size)

DO NOT use @ts-ignore or any type assertions.
</action>
  <verify>File exists with exported component. grep -c "export.*function.*FileList\|export default" src/components/FileList.tsx equals 1</verify>
  <done>FileList table component with type icons, actions, search, and accessibility.</done>
</task>

<task type="auto">
  <name>Task 3: Create src/components/FileBrowserModal.tsx for blog editor</name>
  <files>src/components/FileBrowserModal.tsx</files>
  <action>
Create new modal component following AssetPickerModal.tsx pattern:

Props interface:
- isOpen: boolean
- onClose: () => void
- onSelect: (file: UploadedFile) => void

UI elements:
- Modal with Radix UI Dialog
- Title: "Select File" with FileText icon
- Search input for filename filtering
- File list grid (not table - similar to AssetPickerModal)
- Each file shows: icon, filename, size, upload date
- Click to select and insert markdown link
- Usage count badge if file > 0 uses

File selection behavior:
- Inserts markdown link per D-15: `[File Title](/api/files/download/{id})`
- Or if title exists: `[Title](/api/files/download/{id})`
- Closes modal after selection

Brand compliance:
- ares-cut-lg for modal corners
- ares-gold accent for header
- obsidian background

Accessibility:
- Focus trap in modal
- Escape key closes
- aria-label for file items
- Keyboard navigation through file list

DO NOT use @ts-ignore or any type assertions.
</action>
  <verify>File exists with exported component. grep -c "export.*function.*FileBrowserModal\|export default" src/components/FileBrowserModal.tsx equals 1</verify>
  <done>FileBrowserModal component for blog editor integration with markdown link insertion.</done>
</task>

<task type="auto">
  <name>Task 4: Create src/routes/dashboard/files.tsx page</name>
  <files>src/routes/dashboard/files.tsx</files>
  <action>
Create new dashboard page following src/routes/dashboard/drive-docs.tsx pattern:

Page structure:
- DashboardLayout wrapper
- Header: "File Manager" with FileUploa icon
- Description: "Upload and manage documents for blog posts"
- Two-column layout on desktop:
  - Left: FileUploadZone (top), stats cards (bottom)
  - Right: FileList (full height)
- Search bar for filename filtering
- "Scan Usage" button to trigger file usage scan (shows toast with results)

Stats cards:
- Total files: count from files array
- Total size: sum of file sizes (formatted KB/MB)
- Storage used: percentage visual

State management:
- useFilesQuery for file list
- useUploadMutation for uploads
- useDeleteMutation for deletes
- useScanUsageMutation for usage scanning
- Local search state for filename filtering

Drive import integration per D-09/D-10:
- Reuse Google Drive browser from Phase 74
- Add "Import from Drive" button next to upload zone
- Opens modal with Drive file list
- Add "Import to Files" button to Drive file items
- Uses useImportFromDriveMutation

Brand compliance:
- Page title: text-3xl font-bold text-white tracking-tight
- Stats cards: bg-black/40 border border-white/10 ares-cut-sm
- Labels: text-xs font-bold text-marble/60 uppercase tracking-wider

Accessibility:
- Skip link to main content
- Headings properly nested
- Table accessible
- Keyboard navigation for all actions

DO NOT use @ts-ignore or any type assertions.
</action>
  <verify>File exists with exported page component. grep -c "export.*function.*Files\|export default" src/routes/dashboard/files.tsx equals 1</verify>
  <done>Dashboard file management page with upload, list, search, Drive import, and usage scanning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → UI | Input validation on client side |
| UI → API | Authenticated requests via API client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-16 | Spoofing | File type selection | mitigate | File input accepts attribute restricts to document types; server validates MIME type |
| T-77-17 | Tampering | File size display | accept | Client-side display only; server enforces limit |
| T-77-18 | Information Disclosure | File URLs | mitigate | Download URLs require authentication per D-24; not publicly accessible |
| T-77-19 | Injection | Search query | mitigate | Client-side filter only; no SQL injection risk |

No critical vulnerabilities. Client-side validation is UX convenience; server validates all inputs.
</threat_model>

<verification>
1. FileUploadZone component created with drag-drop
2. FileList component created with table and actions
3. FileBrowserModal component created for blog editor
4. Dashboard page created at src/routes/dashboard/files.tsx
5. All components use ARES brand colors and styling
6. WCAG 2.1 AA compliance: 4.5:1 contrast, keyboard navigability
7. TypeScript compilation succeeds with zero errors
</verification>

<success_criteria>
- User can upload files via drag-drop zone
- User can browse and import from Google Drive
- File list displays with proper columns and actions
- Search filters files by filename
- Download, delete, copy link actions work
- File browser modal ready for blog editor integration
- ARES brand compliance throughout
- WCAG 2.1 AA accessibility compliance
</success_criteria>

<output>
After completion, create `.planning/phases/77-file-manager/77-04-SUMMARY.md`
</output>
