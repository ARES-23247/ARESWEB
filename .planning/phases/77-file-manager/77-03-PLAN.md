---
phase: 77-file-manager
plan: 03
type: execute
wave: 3
depends_on: [77-02]
files_modified:
  - src/api/files.ts
  - src/hooks/useFiles.ts
  - src/hooks/useFiles.test.ts
autonomous: true
requirements: ["FILES-01", "FILES-02", "FILES-05", "FILES-07"]
user_setup: []
must_haves:
  truths:
    - "useFilesQuery fetches file list from GET /api/files"
    - "useUploadMutation uploads files via POST /api/files/upload"
    - "useImportFromDriveMutation imports Drive files via POST /api/files/import-from-drive"
    - "useDeleteMutation deletes files via DELETE /api/files/:id"
    - "useScanUsageMutation triggers usage scan via POST /api/files/scan-usage"
    - "Client functions infer types from OpenAPI contracts"
  artifacts:
    - path: src/api/files.ts
      provides: "Typed API client functions for file management"
      exports: ["useGetFiles", "useUploadFile", "useImportFromDrive", "useDeleteFile", "useScanUsage"]
      min_lines: 80
    - path: src/hooks/useFiles.ts
      provides: "React Query hooks for file management"
      exports: ["useFilesQuery", "useUploadMutation", "useImportFromDriveMutation", "useDeleteMutation", "useScanUsageMutation"]
      min_lines: 50
    - path: src/hooks/useFiles.test.ts
      provides: "Unit tests for file hooks"
  key_links:
    - from: "src/hooks/useFiles.ts"
      to: "shared/routes/files.ts"
      via: "OpenAPI contract import for type inference"
      pattern: "import.*files.*from.*shared/routes"
    - from: "src/api/files.ts"
      to: "functions/api/routes/files/index.ts"
      via: "honoClient fetch wrapper"
      pattern: "honoClient.*files"
---

<objective>
Create type-safe React Query hooks and API client functions for file management.

Purpose: Provide frontend components with typed hooks for file operations, leveraging OpenAPI contracts for end-to-end type inference. Follows existing patterns from src/api/media.ts and src/hooks/useMedia.ts.

Output: API client module and React Query hooks ready for consumption by dashboard UI and blog editor integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-file-manager/77-CONTEXT.md
@.planning/phases/77-file-manager/77-02-PLAN.md
@shared/routes/files.ts
@src/api/media.ts
@src/hooks/useMedia.ts
@src/api/honoClient.ts
@.agents/skills/aresweb-typescript-safety/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/api/files.ts with typed API client functions</name>
  <files>src/api/files.ts</files>
  <action>
Create new file src/api/files.ts following the pattern in src/api/media.ts:

1. Import honoClient from "./honoClient"
2. Import route contracts from "../../shared/routes/files"

3. Define getFiles with:
   - Input: { search?: string }
   - Uses honoClient with listFilesRoute
   - Returns Promise with files array

4. Define uploadFile with:
   - Input: { file: File; title?: string; description?: string }
   - Creates FormData and appends file, title, description
   - Uses honoClient with uploadFileRoute
   - Returns Promise with uploadedFile record

5. Define importFromDrive with:
   - Input: { fileId: string; fileName: string; mimeType: string }
   - Uses honoClient with importFromDriveRoute
   - Returns Promise with uploadedFile record

6. Define deleteFile with:
   - Input: { id: string }
   - Uses honoClient with deleteFileRoute
   - Returns Promise with success boolean

7. Define scanUsage with:
   - Input: {}
   - Uses honoClient with scanUsageRoute
   - Returns Promise with { scanned: number; updated: number }

All functions should leverage honoClient for automatic type inference from route contracts.
DO NOT use @ts-ignore or any type assertions.
</action>
  <verify>File exists with exported functions. grep -c "export.*function.*File\|export.*const.*File" src/api/files.ts greater than 0</verify>
  <done>Typed API client functions exported. Types inferred from OpenAPI contracts. Ready for React Query wrapper hooks.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/hooks/useFiles.ts with React Query hooks</name>
  <files>src/hooks/useFiles.ts</files>
  <action>
Create new file src/hooks/useFiles.ts following the pattern in src/hooks/useMedia.ts:

1. Import useQuery, useMutation from "@tanstack/react-query"
2. Import api functions from "../api/files"
3. Import client from "../api/honoClient"

4. Export useFilesQuery:
   - Uses useQuery with queryKey ["files", search?]
   - Calls api.getFiles
   - Returns { data, isLoading, error, refetch }

5. Export useUploadMutation:
   - Uses useMutation with api.uploadFile
   - onSuccess: Invalidate ["files"] query
   - Returns { mutate, isPending, error }

6. Export useImportFromDriveMutation:
   - Uses useMutation with api.importFromDrive
   - onSuccess: Invalidate ["files"] query
   - Returns { mutate, isPending, error }

7. Export useDeleteMutation:
   - Uses useMutation with api.deleteFile
   - onSuccess: Invalidate ["files"] query
   - Returns { mutate, isPending, error }

8. Export useScanUsageMutation:
   - Uses useMutation with api.scanUsage
   - onSuccess: Invalidate ["files"] query
   - Returns { mutate, isPending, error }

All hooks should provide proper TypeScript types via inference from api functions.
DO NOT use @ts-ignore or any type assertions.
</action>
  <verify>File exists with exported hooks. grep -c "export.*use.*Query\|export.*use.*Mutation" src/hooks/useFiles.ts greater than 0</verify>
  <done>React Query hooks exported. Components can now use typed file operations with automatic cache management.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add unit tests for file hooks</name>
  <files>src/hooks/useFiles.test.ts</files>
  <behavior>
    Test 1: useFilesQuery returns data from getFiles API
    Test 2: useFilesQuery includes search parameter in queryKey
    Test 3: useFilesQuery sets isLoading to true during fetch
    Test 4: useUploadMutation calls uploadFile API with FormData
    Test 5: useUploadMutation invalidates files query on success
    Test 6: useImportFromDriveMutation calls importFromDrive API
    Test 7: useImportFromDriveMutation invalidates files query on success
    Test 8: useDeleteMutation calls deleteFile API with id
    Test 9: useDeleteMutation invalidates files query on success
    Test 10: useScanUsageMutation calls scanUsage API
  </behavior>
  <action>Create test file src/hooks/useFiles.test.ts following vitest patterns. Mock honoClient and test each hook's behavior. Verify query keys, mutation calls, and cache invalidation.</action>
  <verify>npm test -- src/hooks/useFiles.test.ts passes</verify>
  <done>Unit tests pass. All file hooks tested for API calls, query keys, and cache invalidation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React Hooks → API Client | Props and callbacks cross here |
| API Client → Backend | Typed requests via honoClient |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-13 | Injection | FormData construction | mitigate | FormData API handles encoding; no manual string concatenation |
| T-77-14 | Information Disclosure | Error handling | accept | Errors passed through from honoClient; sensitive details filtered at backend |
| T-77-15 | Spoofing | Query invalidation | accept | React Query cache invalidation is client-side only; no security impact |

No new threats. Client code follows established patterns.
</threat_model>

<verification>
1. src/api/files.ts exists with exported API functions
2. src/hooks/useFiles.ts exists with exported React Query hooks
3. All functions use honoClient for type inference
4. Query keys properly structured for cache management
5. Mutations invalidate relevant queries on success
6. Unit tests pass for all hooks
7. TypeScript compilation succeeds with zero errors
</verification>

<success_criteria>
- Type-safe API client functions created
- React Query hooks wrap API functions
- Query keys properly structured
- Cache invalidation configured
- Unit tests pass
- Frontend ready for dashboard and blog editor integration
</success_criteria>

<output>
After completion, create `.planning/phases/77-file-manager/77-03-SUMMARY.md`
</output>
