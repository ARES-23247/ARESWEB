rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    function isAuthorized() {
      return request.auth != null && firestore.exists(/databases/(default)/documents/authorized_users/$(request.auth.token.email.lower()));
    }

    match /events/{eventId}/photos/{photoId} {
      allow read: if true;
      allow write: if isAuthorized();
    }

    match /gallery/{allPaths=**} {
      allow read: if true;
      allow write: if isAuthorized();
    }

    match /cad/{allPaths=**} {
      allow read: if true;
      allow write: if isAuthorized();
    }

    match /telemetry_runs/{allPaths=**} {
      allow read: if isAuthorized();
      allow write: if isAuthorized();
    }
  }
}
