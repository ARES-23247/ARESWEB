rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /events/{eventId}/photos/{photoId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
